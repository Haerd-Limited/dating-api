// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/aarondl/randomize"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testScoringPenalties(t *testing.T) {
	t.Parallel()

	query := ScoringPenalties()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testScoringPenaltiesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringPenalty{}
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ScoringPenalties().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testScoringPenaltiesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringPenalty{}
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := ScoringPenalties().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ScoringPenalties().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testScoringPenaltiesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringPenalty{}
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ScoringPenaltySlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ScoringPenalties().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testScoringPenaltiesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringPenalty{}
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := ScoringPenaltyExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if ScoringPenalty exists: %s", err)
	}
	if !e {
		t.Errorf("Expected ScoringPenaltyExists to return true, but got false.")
	}
}

func testScoringPenaltiesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringPenalty{}
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	scoringPenaltyFound, err := FindScoringPenalty(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if scoringPenaltyFound == nil {
		t.Error("want a record, got nil")
	}
}

func testScoringPenaltiesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringPenalty{}
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = ScoringPenalties().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testScoringPenaltiesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringPenalty{}
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := ScoringPenalties().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testScoringPenaltiesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	scoringPenaltyOne := &ScoringPenalty{}
	scoringPenaltyTwo := &ScoringPenalty{}
	if err = randomize.Struct(seed, scoringPenaltyOne, scoringPenaltyDBTypes, false, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}
	if err = randomize.Struct(seed, scoringPenaltyTwo, scoringPenaltyDBTypes, false, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = scoringPenaltyOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = scoringPenaltyTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := ScoringPenalties().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testScoringPenaltiesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	scoringPenaltyOne := &ScoringPenalty{}
	scoringPenaltyTwo := &ScoringPenalty{}
	if err = randomize.Struct(seed, scoringPenaltyOne, scoringPenaltyDBTypes, false, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}
	if err = randomize.Struct(seed, scoringPenaltyTwo, scoringPenaltyDBTypes, false, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = scoringPenaltyOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = scoringPenaltyTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ScoringPenalties().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func scoringPenaltyBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *ScoringPenalty) error {
	*o = ScoringPenalty{}
	return nil
}

func scoringPenaltyAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *ScoringPenalty) error {
	*o = ScoringPenalty{}
	return nil
}

func scoringPenaltyAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *ScoringPenalty) error {
	*o = ScoringPenalty{}
	return nil
}

func scoringPenaltyBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *ScoringPenalty) error {
	*o = ScoringPenalty{}
	return nil
}

func scoringPenaltyAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *ScoringPenalty) error {
	*o = ScoringPenalty{}
	return nil
}

func scoringPenaltyBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *ScoringPenalty) error {
	*o = ScoringPenalty{}
	return nil
}

func scoringPenaltyAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *ScoringPenalty) error {
	*o = ScoringPenalty{}
	return nil
}

func scoringPenaltyBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *ScoringPenalty) error {
	*o = ScoringPenalty{}
	return nil
}

func scoringPenaltyAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *ScoringPenalty) error {
	*o = ScoringPenalty{}
	return nil
}

func testScoringPenaltiesHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &ScoringPenalty{}
	o := &ScoringPenalty{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, false); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty object: %s", err)
	}

	AddScoringPenaltyHook(boil.BeforeInsertHook, scoringPenaltyBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	scoringPenaltyBeforeInsertHooks = []ScoringPenaltyHook{}

	AddScoringPenaltyHook(boil.AfterInsertHook, scoringPenaltyAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	scoringPenaltyAfterInsertHooks = []ScoringPenaltyHook{}

	AddScoringPenaltyHook(boil.AfterSelectHook, scoringPenaltyAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	scoringPenaltyAfterSelectHooks = []ScoringPenaltyHook{}

	AddScoringPenaltyHook(boil.BeforeUpdateHook, scoringPenaltyBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	scoringPenaltyBeforeUpdateHooks = []ScoringPenaltyHook{}

	AddScoringPenaltyHook(boil.AfterUpdateHook, scoringPenaltyAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	scoringPenaltyAfterUpdateHooks = []ScoringPenaltyHook{}

	AddScoringPenaltyHook(boil.BeforeDeleteHook, scoringPenaltyBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	scoringPenaltyBeforeDeleteHooks = []ScoringPenaltyHook{}

	AddScoringPenaltyHook(boil.AfterDeleteHook, scoringPenaltyAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	scoringPenaltyAfterDeleteHooks = []ScoringPenaltyHook{}

	AddScoringPenaltyHook(boil.BeforeUpsertHook, scoringPenaltyBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	scoringPenaltyBeforeUpsertHooks = []ScoringPenaltyHook{}

	AddScoringPenaltyHook(boil.AfterUpsertHook, scoringPenaltyAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	scoringPenaltyAfterUpsertHooks = []ScoringPenaltyHook{}
}

func testScoringPenaltiesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringPenalty{}
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ScoringPenalties().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testScoringPenaltiesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringPenalty{}
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(strmangle.SetMerge(scoringPenaltyPrimaryKeyColumns, scoringPenaltyColumnsWithoutDefault)...)); err != nil {
		t.Error(err)
	}

	count, err := ScoringPenalties().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testScoringPenaltiesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringPenalty{}
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testScoringPenaltiesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringPenalty{}
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ScoringPenaltySlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testScoringPenaltiesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringPenalty{}
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := ScoringPenalties().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	scoringPenaltyDBTypes = map[string]string{`ID`: `bigint`, `DuplicateWindowSeconds`: `integer`, `MaxMSGSPerMinute`: `integer`, `CreatedAt`: `timestamp with time zone`}
	_                     = bytes.MinRead
)

func testScoringPenaltiesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(scoringPenaltyPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(scoringPenaltyAllColumns) == len(scoringPenaltyPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &ScoringPenalty{}
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ScoringPenalties().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testScoringPenaltiesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(scoringPenaltyAllColumns) == len(scoringPenaltyPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &ScoringPenalty{}
	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ScoringPenalties().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, scoringPenaltyDBTypes, true, scoringPenaltyPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(scoringPenaltyAllColumns, scoringPenaltyPrimaryKeyColumns) {
		fields = scoringPenaltyAllColumns
	} else {
		fields = strmangle.SetComplement(
			scoringPenaltyAllColumns,
			scoringPenaltyPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := ScoringPenaltySlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testScoringPenaltiesUpsert(t *testing.T) {
	t.Parallel()

	if len(scoringPenaltyAllColumns) == len(scoringPenaltyPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := ScoringPenalty{}
	if err = randomize.Struct(seed, &o, scoringPenaltyDBTypes, true); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert ScoringPenalty: %s", err)
	}

	count, err := ScoringPenalties().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, scoringPenaltyDBTypes, false, scoringPenaltyPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ScoringPenalty struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert ScoringPenalty: %s", err)
	}

	count, err = ScoringPenalties().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
