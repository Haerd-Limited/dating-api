// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/aarondl/randomize"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testVoiceWorthHearingWeeklies(t *testing.T) {
	t.Parallel()

	query := VoiceWorthHearingWeeklies()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testVoiceWorthHearingWeekliesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := VoiceWorthHearingWeeklies().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testVoiceWorthHearingWeekliesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := VoiceWorthHearingWeeklies().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := VoiceWorthHearingWeeklies().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testVoiceWorthHearingWeekliesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := VoiceWorthHearingWeeklySlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := VoiceWorthHearingWeeklies().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testVoiceWorthHearingWeekliesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := VoiceWorthHearingWeeklyExists(ctx, tx, o.UserID, o.WeekStart)
	if err != nil {
		t.Errorf("Unable to check if VoiceWorthHearingWeekly exists: %s", err)
	}
	if !e {
		t.Errorf("Expected VoiceWorthHearingWeeklyExists to return true, but got false.")
	}
}

func testVoiceWorthHearingWeekliesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	voiceWorthHearingWeeklyFound, err := FindVoiceWorthHearingWeekly(ctx, tx, o.UserID, o.WeekStart)
	if err != nil {
		t.Error(err)
	}

	if voiceWorthHearingWeeklyFound == nil {
		t.Error("want a record, got nil")
	}
}

func testVoiceWorthHearingWeekliesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = VoiceWorthHearingWeeklies().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testVoiceWorthHearingWeekliesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := VoiceWorthHearingWeeklies().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testVoiceWorthHearingWeekliesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	voiceWorthHearingWeeklyOne := &VoiceWorthHearingWeekly{}
	voiceWorthHearingWeeklyTwo := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, voiceWorthHearingWeeklyOne, voiceWorthHearingWeeklyDBTypes, false, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}
	if err = randomize.Struct(seed, voiceWorthHearingWeeklyTwo, voiceWorthHearingWeeklyDBTypes, false, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = voiceWorthHearingWeeklyOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = voiceWorthHearingWeeklyTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := VoiceWorthHearingWeeklies().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testVoiceWorthHearingWeekliesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	voiceWorthHearingWeeklyOne := &VoiceWorthHearingWeekly{}
	voiceWorthHearingWeeklyTwo := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, voiceWorthHearingWeeklyOne, voiceWorthHearingWeeklyDBTypes, false, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}
	if err = randomize.Struct(seed, voiceWorthHearingWeeklyTwo, voiceWorthHearingWeeklyDBTypes, false, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = voiceWorthHearingWeeklyOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = voiceWorthHearingWeeklyTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := VoiceWorthHearingWeeklies().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func voiceWorthHearingWeeklyBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *VoiceWorthHearingWeekly) error {
	*o = VoiceWorthHearingWeekly{}
	return nil
}

func voiceWorthHearingWeeklyAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *VoiceWorthHearingWeekly) error {
	*o = VoiceWorthHearingWeekly{}
	return nil
}

func voiceWorthHearingWeeklyAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *VoiceWorthHearingWeekly) error {
	*o = VoiceWorthHearingWeekly{}
	return nil
}

func voiceWorthHearingWeeklyBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *VoiceWorthHearingWeekly) error {
	*o = VoiceWorthHearingWeekly{}
	return nil
}

func voiceWorthHearingWeeklyAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *VoiceWorthHearingWeekly) error {
	*o = VoiceWorthHearingWeekly{}
	return nil
}

func voiceWorthHearingWeeklyBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *VoiceWorthHearingWeekly) error {
	*o = VoiceWorthHearingWeekly{}
	return nil
}

func voiceWorthHearingWeeklyAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *VoiceWorthHearingWeekly) error {
	*o = VoiceWorthHearingWeekly{}
	return nil
}

func voiceWorthHearingWeeklyBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *VoiceWorthHearingWeekly) error {
	*o = VoiceWorthHearingWeekly{}
	return nil
}

func voiceWorthHearingWeeklyAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *VoiceWorthHearingWeekly) error {
	*o = VoiceWorthHearingWeekly{}
	return nil
}

func testVoiceWorthHearingWeekliesHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &VoiceWorthHearingWeekly{}
	o := &VoiceWorthHearingWeekly{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, false); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly object: %s", err)
	}

	AddVoiceWorthHearingWeeklyHook(boil.BeforeInsertHook, voiceWorthHearingWeeklyBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	voiceWorthHearingWeeklyBeforeInsertHooks = []VoiceWorthHearingWeeklyHook{}

	AddVoiceWorthHearingWeeklyHook(boil.AfterInsertHook, voiceWorthHearingWeeklyAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	voiceWorthHearingWeeklyAfterInsertHooks = []VoiceWorthHearingWeeklyHook{}

	AddVoiceWorthHearingWeeklyHook(boil.AfterSelectHook, voiceWorthHearingWeeklyAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	voiceWorthHearingWeeklyAfterSelectHooks = []VoiceWorthHearingWeeklyHook{}

	AddVoiceWorthHearingWeeklyHook(boil.BeforeUpdateHook, voiceWorthHearingWeeklyBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	voiceWorthHearingWeeklyBeforeUpdateHooks = []VoiceWorthHearingWeeklyHook{}

	AddVoiceWorthHearingWeeklyHook(boil.AfterUpdateHook, voiceWorthHearingWeeklyAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	voiceWorthHearingWeeklyAfterUpdateHooks = []VoiceWorthHearingWeeklyHook{}

	AddVoiceWorthHearingWeeklyHook(boil.BeforeDeleteHook, voiceWorthHearingWeeklyBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	voiceWorthHearingWeeklyBeforeDeleteHooks = []VoiceWorthHearingWeeklyHook{}

	AddVoiceWorthHearingWeeklyHook(boil.AfterDeleteHook, voiceWorthHearingWeeklyAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	voiceWorthHearingWeeklyAfterDeleteHooks = []VoiceWorthHearingWeeklyHook{}

	AddVoiceWorthHearingWeeklyHook(boil.BeforeUpsertHook, voiceWorthHearingWeeklyBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	voiceWorthHearingWeeklyBeforeUpsertHooks = []VoiceWorthHearingWeeklyHook{}

	AddVoiceWorthHearingWeeklyHook(boil.AfterUpsertHook, voiceWorthHearingWeeklyAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	voiceWorthHearingWeeklyAfterUpsertHooks = []VoiceWorthHearingWeeklyHook{}
}

func testVoiceWorthHearingWeekliesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := VoiceWorthHearingWeeklies().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testVoiceWorthHearingWeekliesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(strmangle.SetMerge(voiceWorthHearingWeeklyPrimaryKeyColumns, voiceWorthHearingWeeklyColumnsWithoutDefault)...)); err != nil {
		t.Error(err)
	}

	count, err := VoiceWorthHearingWeeklies().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testVoiceWorthHearingWeeklyToOneUserUsingUser(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local VoiceWorthHearingWeekly
	var foreign User

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, voiceWorthHearingWeeklyDBTypes, false, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, userDBTypes, false, userColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize User struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.UserID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.User().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	ranAfterSelectHook := false
	AddUserHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *User) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := VoiceWorthHearingWeeklySlice{&local}
	if err = local.L.LoadUser(ctx, tx, false, (*[]*VoiceWorthHearingWeekly)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.User = nil
	if err = local.L.LoadUser(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testVoiceWorthHearingWeeklyToOneSetOpUserUsingUser(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a VoiceWorthHearingWeekly
	var b, c User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, voiceWorthHearingWeeklyDBTypes, false, strmangle.SetComplement(voiceWorthHearingWeeklyPrimaryKeyColumns, voiceWorthHearingWeeklyColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*User{&b, &c} {
		err = a.SetUser(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.User != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.VoiceWorthHearingWeeklies[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.UserID != x.ID {
			t.Error("foreign key was wrong value", a.UserID)
		}

		if exists, err := VoiceWorthHearingWeeklyExists(ctx, tx, a.UserID, a.WeekStart); err != nil {
			t.Fatal(err)
		} else if !exists {
			t.Error("want 'a' to exist")
		}

	}
}

func testVoiceWorthHearingWeekliesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testVoiceWorthHearingWeekliesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := VoiceWorthHearingWeeklySlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testVoiceWorthHearingWeekliesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := VoiceWorthHearingWeeklies().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	voiceWorthHearingWeeklyDBTypes = map[string]string{`UserID`: `uuid`, `WeekStart`: `date`, `CandidateIds`: `ARRAYuuid`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`}
	_                              = bytes.MinRead
)

func testVoiceWorthHearingWeekliesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(voiceWorthHearingWeeklyPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(voiceWorthHearingWeeklyAllColumns) == len(voiceWorthHearingWeeklyPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := VoiceWorthHearingWeeklies().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testVoiceWorthHearingWeekliesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(voiceWorthHearingWeeklyAllColumns) == len(voiceWorthHearingWeeklyPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := VoiceWorthHearingWeeklies().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, voiceWorthHearingWeeklyDBTypes, true, voiceWorthHearingWeeklyPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(voiceWorthHearingWeeklyAllColumns, voiceWorthHearingWeeklyPrimaryKeyColumns) {
		fields = voiceWorthHearingWeeklyAllColumns
	} else {
		fields = strmangle.SetComplement(
			voiceWorthHearingWeeklyAllColumns,
			voiceWorthHearingWeeklyPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := VoiceWorthHearingWeeklySlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testVoiceWorthHearingWeekliesUpsert(t *testing.T) {
	t.Parallel()

	if len(voiceWorthHearingWeeklyAllColumns) == len(voiceWorthHearingWeeklyPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := VoiceWorthHearingWeekly{}
	if err = randomize.Struct(seed, &o, voiceWorthHearingWeeklyDBTypes, true); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert VoiceWorthHearingWeekly: %s", err)
	}

	count, err := VoiceWorthHearingWeeklies().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, voiceWorthHearingWeeklyDBTypes, false, voiceWorthHearingWeeklyPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize VoiceWorthHearingWeekly struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert VoiceWorthHearingWeekly: %s", err)
	}

	count, err = VoiceWorthHearingWeeklies().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
