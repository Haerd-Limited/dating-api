// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// MatchSlotWatch is an object representing the database table.
type MatchSlotWatch struct {
	ID            string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	WatcherUserID string    `boil:"watcher_user_id" json:"watcher_user_id" toml:"watcher_user_id" yaml:"watcher_user_id"`
	WatchedUserID string    `boil:"watched_user_id" json:"watched_user_id" toml:"watched_user_id" yaml:"watched_user_id"`
	CreatedAt     time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *matchSlotWatchR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L matchSlotWatchL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MatchSlotWatchColumns = struct {
	ID            string
	WatcherUserID string
	WatchedUserID string
	CreatedAt     string
}{
	ID:            "id",
	WatcherUserID: "watcher_user_id",
	WatchedUserID: "watched_user_id",
	CreatedAt:     "created_at",
}

var MatchSlotWatchTableColumns = struct {
	ID            string
	WatcherUserID string
	WatchedUserID string
	CreatedAt     string
}{
	ID:            "match_slot_watches.id",
	WatcherUserID: "match_slot_watches.watcher_user_id",
	WatchedUserID: "match_slot_watches.watched_user_id",
	CreatedAt:     "match_slot_watches.created_at",
}

// Generated where

var MatchSlotWatchWhere = struct {
	ID            whereHelperstring
	WatcherUserID whereHelperstring
	WatchedUserID whereHelperstring
	CreatedAt     whereHelpertime_Time
}{
	ID:            whereHelperstring{field: "\"match_slot_watches\".\"id\""},
	WatcherUserID: whereHelperstring{field: "\"match_slot_watches\".\"watcher_user_id\""},
	WatchedUserID: whereHelperstring{field: "\"match_slot_watches\".\"watched_user_id\""},
	CreatedAt:     whereHelpertime_Time{field: "\"match_slot_watches\".\"created_at\""},
}

// MatchSlotWatchRels is where relationship names are stored.
var MatchSlotWatchRels = struct {
	WatchedUser string
	WatcherUser string
}{
	WatchedUser: "WatchedUser",
	WatcherUser: "WatcherUser",
}

// matchSlotWatchR is where relationships are stored.
type matchSlotWatchR struct {
	WatchedUser *User `boil:"WatchedUser" json:"WatchedUser" toml:"WatchedUser" yaml:"WatchedUser"`
	WatcherUser *User `boil:"WatcherUser" json:"WatcherUser" toml:"WatcherUser" yaml:"WatcherUser"`
}

// NewStruct creates a new relationship struct
func (*matchSlotWatchR) NewStruct() *matchSlotWatchR {
	return &matchSlotWatchR{}
}

func (o *MatchSlotWatch) GetWatchedUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetWatchedUser()
}

func (r *matchSlotWatchR) GetWatchedUser() *User {
	if r == nil {
		return nil
	}

	return r.WatchedUser
}

func (o *MatchSlotWatch) GetWatcherUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetWatcherUser()
}

func (r *matchSlotWatchR) GetWatcherUser() *User {
	if r == nil {
		return nil
	}

	return r.WatcherUser
}

// matchSlotWatchL is where Load methods for each relationship are stored.
type matchSlotWatchL struct{}

var (
	matchSlotWatchAllColumns            = []string{"id", "watcher_user_id", "watched_user_id", "created_at"}
	matchSlotWatchColumnsWithoutDefault = []string{"watcher_user_id", "watched_user_id"}
	matchSlotWatchColumnsWithDefault    = []string{"id", "created_at"}
	matchSlotWatchPrimaryKeyColumns     = []string{"id"}
	matchSlotWatchGeneratedColumns      = []string{}
)

type (
	// MatchSlotWatchSlice is an alias for a slice of pointers to MatchSlotWatch.
	// This should almost always be used instead of []MatchSlotWatch.
	MatchSlotWatchSlice []*MatchSlotWatch
	// MatchSlotWatchHook is the signature for custom MatchSlotWatch hook methods
	MatchSlotWatchHook func(context.Context, boil.ContextExecutor, *MatchSlotWatch) error

	matchSlotWatchQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	matchSlotWatchType                 = reflect.TypeOf(&MatchSlotWatch{})
	matchSlotWatchMapping              = queries.MakeStructMapping(matchSlotWatchType)
	matchSlotWatchPrimaryKeyMapping, _ = queries.BindMapping(matchSlotWatchType, matchSlotWatchMapping, matchSlotWatchPrimaryKeyColumns)
	matchSlotWatchInsertCacheMut       sync.RWMutex
	matchSlotWatchInsertCache          = make(map[string]insertCache)
	matchSlotWatchUpdateCacheMut       sync.RWMutex
	matchSlotWatchUpdateCache          = make(map[string]updateCache)
	matchSlotWatchUpsertCacheMut       sync.RWMutex
	matchSlotWatchUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var matchSlotWatchAfterSelectMu sync.Mutex
var matchSlotWatchAfterSelectHooks []MatchSlotWatchHook

var matchSlotWatchBeforeInsertMu sync.Mutex
var matchSlotWatchBeforeInsertHooks []MatchSlotWatchHook
var matchSlotWatchAfterInsertMu sync.Mutex
var matchSlotWatchAfterInsertHooks []MatchSlotWatchHook

var matchSlotWatchBeforeUpdateMu sync.Mutex
var matchSlotWatchBeforeUpdateHooks []MatchSlotWatchHook
var matchSlotWatchAfterUpdateMu sync.Mutex
var matchSlotWatchAfterUpdateHooks []MatchSlotWatchHook

var matchSlotWatchBeforeDeleteMu sync.Mutex
var matchSlotWatchBeforeDeleteHooks []MatchSlotWatchHook
var matchSlotWatchAfterDeleteMu sync.Mutex
var matchSlotWatchAfterDeleteHooks []MatchSlotWatchHook

var matchSlotWatchBeforeUpsertMu sync.Mutex
var matchSlotWatchBeforeUpsertHooks []MatchSlotWatchHook
var matchSlotWatchAfterUpsertMu sync.Mutex
var matchSlotWatchAfterUpsertHooks []MatchSlotWatchHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MatchSlotWatch) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range matchSlotWatchAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MatchSlotWatch) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range matchSlotWatchBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MatchSlotWatch) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range matchSlotWatchAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MatchSlotWatch) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range matchSlotWatchBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MatchSlotWatch) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range matchSlotWatchAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MatchSlotWatch) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range matchSlotWatchBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MatchSlotWatch) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range matchSlotWatchAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MatchSlotWatch) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range matchSlotWatchBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MatchSlotWatch) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range matchSlotWatchAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMatchSlotWatchHook registers your hook function for all future operations.
func AddMatchSlotWatchHook(hookPoint boil.HookPoint, matchSlotWatchHook MatchSlotWatchHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		matchSlotWatchAfterSelectMu.Lock()
		matchSlotWatchAfterSelectHooks = append(matchSlotWatchAfterSelectHooks, matchSlotWatchHook)
		matchSlotWatchAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		matchSlotWatchBeforeInsertMu.Lock()
		matchSlotWatchBeforeInsertHooks = append(matchSlotWatchBeforeInsertHooks, matchSlotWatchHook)
		matchSlotWatchBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		matchSlotWatchAfterInsertMu.Lock()
		matchSlotWatchAfterInsertHooks = append(matchSlotWatchAfterInsertHooks, matchSlotWatchHook)
		matchSlotWatchAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		matchSlotWatchBeforeUpdateMu.Lock()
		matchSlotWatchBeforeUpdateHooks = append(matchSlotWatchBeforeUpdateHooks, matchSlotWatchHook)
		matchSlotWatchBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		matchSlotWatchAfterUpdateMu.Lock()
		matchSlotWatchAfterUpdateHooks = append(matchSlotWatchAfterUpdateHooks, matchSlotWatchHook)
		matchSlotWatchAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		matchSlotWatchBeforeDeleteMu.Lock()
		matchSlotWatchBeforeDeleteHooks = append(matchSlotWatchBeforeDeleteHooks, matchSlotWatchHook)
		matchSlotWatchBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		matchSlotWatchAfterDeleteMu.Lock()
		matchSlotWatchAfterDeleteHooks = append(matchSlotWatchAfterDeleteHooks, matchSlotWatchHook)
		matchSlotWatchAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		matchSlotWatchBeforeUpsertMu.Lock()
		matchSlotWatchBeforeUpsertHooks = append(matchSlotWatchBeforeUpsertHooks, matchSlotWatchHook)
		matchSlotWatchBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		matchSlotWatchAfterUpsertMu.Lock()
		matchSlotWatchAfterUpsertHooks = append(matchSlotWatchAfterUpsertHooks, matchSlotWatchHook)
		matchSlotWatchAfterUpsertMu.Unlock()
	}
}

// One returns a single matchSlotWatch record from the query.
func (q matchSlotWatchQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MatchSlotWatch, error) {
	o := &MatchSlotWatch{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: failed to execute a one query for match_slot_watches")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all MatchSlotWatch records from the query.
func (q matchSlotWatchQuery) All(ctx context.Context, exec boil.ContextExecutor) (MatchSlotWatchSlice, error) {
	var o []*MatchSlotWatch

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "entity: failed to assign all query results to MatchSlotWatch slice")
	}

	if len(matchSlotWatchAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all MatchSlotWatch records in the query.
func (q matchSlotWatchQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to count match_slot_watches rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q matchSlotWatchQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "entity: failed to check if match_slot_watches exists")
	}

	return count > 0, nil
}

// WatchedUser pointed to by the foreign key.
func (o *MatchSlotWatch) WatchedUser(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WatchedUserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// WatcherUser pointed to by the foreign key.
func (o *MatchSlotWatch) WatcherUser(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WatcherUserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadWatchedUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (matchSlotWatchL) LoadWatchedUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeMatchSlotWatch interface{}, mods queries.Applicator) error {
	var slice []*MatchSlotWatch
	var object *MatchSlotWatch

	if singular {
		var ok bool
		object, ok = maybeMatchSlotWatch.(*MatchSlotWatch)
		if !ok {
			object = new(MatchSlotWatch)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeMatchSlotWatch)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeMatchSlotWatch))
			}
		}
	} else {
		s, ok := maybeMatchSlotWatch.(*[]*MatchSlotWatch)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeMatchSlotWatch)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeMatchSlotWatch))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &matchSlotWatchR{}
		}
		args[object.WatchedUserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &matchSlotWatchR{}
			}

			args[obj.WatchedUserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.WatchedUser = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.WatchedUserMatchSlotWatches = append(foreign.R.WatchedUserMatchSlotWatches, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WatchedUserID == foreign.ID {
				local.R.WatchedUser = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.WatchedUserMatchSlotWatches = append(foreign.R.WatchedUserMatchSlotWatches, local)
				break
			}
		}
	}

	return nil
}

// LoadWatcherUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (matchSlotWatchL) LoadWatcherUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeMatchSlotWatch interface{}, mods queries.Applicator) error {
	var slice []*MatchSlotWatch
	var object *MatchSlotWatch

	if singular {
		var ok bool
		object, ok = maybeMatchSlotWatch.(*MatchSlotWatch)
		if !ok {
			object = new(MatchSlotWatch)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeMatchSlotWatch)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeMatchSlotWatch))
			}
		}
	} else {
		s, ok := maybeMatchSlotWatch.(*[]*MatchSlotWatch)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeMatchSlotWatch)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeMatchSlotWatch))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &matchSlotWatchR{}
		}
		args[object.WatcherUserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &matchSlotWatchR{}
			}

			args[obj.WatcherUserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.WatcherUser = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.WatcherUserMatchSlotWatches = append(foreign.R.WatcherUserMatchSlotWatches, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WatcherUserID == foreign.ID {
				local.R.WatcherUser = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.WatcherUserMatchSlotWatches = append(foreign.R.WatcherUserMatchSlotWatches, local)
				break
			}
		}
	}

	return nil
}

// SetWatchedUser of the matchSlotWatch to the related item.
// Sets o.R.WatchedUser to related.
// Adds o to related.R.WatchedUserMatchSlotWatches.
func (o *MatchSlotWatch) SetWatchedUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"match_slot_watches\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"watched_user_id"}),
		strmangle.WhereClause("\"", "\"", 2, matchSlotWatchPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WatchedUserID = related.ID
	if o.R == nil {
		o.R = &matchSlotWatchR{
			WatchedUser: related,
		}
	} else {
		o.R.WatchedUser = related
	}

	if related.R == nil {
		related.R = &userR{
			WatchedUserMatchSlotWatches: MatchSlotWatchSlice{o},
		}
	} else {
		related.R.WatchedUserMatchSlotWatches = append(related.R.WatchedUserMatchSlotWatches, o)
	}

	return nil
}

// SetWatcherUser of the matchSlotWatch to the related item.
// Sets o.R.WatcherUser to related.
// Adds o to related.R.WatcherUserMatchSlotWatches.
func (o *MatchSlotWatch) SetWatcherUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"match_slot_watches\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"watcher_user_id"}),
		strmangle.WhereClause("\"", "\"", 2, matchSlotWatchPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WatcherUserID = related.ID
	if o.R == nil {
		o.R = &matchSlotWatchR{
			WatcherUser: related,
		}
	} else {
		o.R.WatcherUser = related
	}

	if related.R == nil {
		related.R = &userR{
			WatcherUserMatchSlotWatches: MatchSlotWatchSlice{o},
		}
	} else {
		related.R.WatcherUserMatchSlotWatches = append(related.R.WatcherUserMatchSlotWatches, o)
	}

	return nil
}

// MatchSlotWatches retrieves all the records using an executor.
func MatchSlotWatches(mods ...qm.QueryMod) matchSlotWatchQuery {
	mods = append(mods, qm.From("\"match_slot_watches\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"match_slot_watches\".*"})
	}

	return matchSlotWatchQuery{q}
}

// FindMatchSlotWatch retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMatchSlotWatch(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*MatchSlotWatch, error) {
	matchSlotWatchObj := &MatchSlotWatch{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"match_slot_watches\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, matchSlotWatchObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: unable to select from match_slot_watches")
	}

	if err = matchSlotWatchObj.doAfterSelectHooks(ctx, exec); err != nil {
		return matchSlotWatchObj, err
	}

	return matchSlotWatchObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MatchSlotWatch) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("entity: no match_slot_watches provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(matchSlotWatchColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	matchSlotWatchInsertCacheMut.RLock()
	cache, cached := matchSlotWatchInsertCache[key]
	matchSlotWatchInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			matchSlotWatchAllColumns,
			matchSlotWatchColumnsWithDefault,
			matchSlotWatchColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(matchSlotWatchType, matchSlotWatchMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(matchSlotWatchType, matchSlotWatchMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"match_slot_watches\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"match_slot_watches\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "entity: unable to insert into match_slot_watches")
	}

	if !cached {
		matchSlotWatchInsertCacheMut.Lock()
		matchSlotWatchInsertCache[key] = cache
		matchSlotWatchInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the MatchSlotWatch.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MatchSlotWatch) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	matchSlotWatchUpdateCacheMut.RLock()
	cache, cached := matchSlotWatchUpdateCache[key]
	matchSlotWatchUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			matchSlotWatchAllColumns,
			matchSlotWatchPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("entity: unable to update match_slot_watches, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"match_slot_watches\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, matchSlotWatchPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(matchSlotWatchType, matchSlotWatchMapping, append(wl, matchSlotWatchPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update match_slot_watches row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by update for match_slot_watches")
	}

	if !cached {
		matchSlotWatchUpdateCacheMut.Lock()
		matchSlotWatchUpdateCache[key] = cache
		matchSlotWatchUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q matchSlotWatchQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all for match_slot_watches")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected for match_slot_watches")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MatchSlotWatchSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("entity: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), matchSlotWatchPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"match_slot_watches\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, matchSlotWatchPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all in matchSlotWatch slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected all in update all matchSlotWatch")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MatchSlotWatch) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("entity: no match_slot_watches provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(matchSlotWatchColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	matchSlotWatchUpsertCacheMut.RLock()
	cache, cached := matchSlotWatchUpsertCache[key]
	matchSlotWatchUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			matchSlotWatchAllColumns,
			matchSlotWatchColumnsWithDefault,
			matchSlotWatchColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			matchSlotWatchAllColumns,
			matchSlotWatchPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("entity: unable to upsert match_slot_watches, could not build update column list")
		}

		ret := strmangle.SetComplement(matchSlotWatchAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(matchSlotWatchPrimaryKeyColumns) == 0 {
				return errors.New("entity: unable to upsert match_slot_watches, could not build conflict column list")
			}

			conflict = make([]string, len(matchSlotWatchPrimaryKeyColumns))
			copy(conflict, matchSlotWatchPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"match_slot_watches\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(matchSlotWatchType, matchSlotWatchMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(matchSlotWatchType, matchSlotWatchMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "entity: unable to upsert match_slot_watches")
	}

	if !cached {
		matchSlotWatchUpsertCacheMut.Lock()
		matchSlotWatchUpsertCache[key] = cache
		matchSlotWatchUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single MatchSlotWatch record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MatchSlotWatch) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("entity: no MatchSlotWatch provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), matchSlotWatchPrimaryKeyMapping)
	sql := "DELETE FROM \"match_slot_watches\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete from match_slot_watches")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by delete for match_slot_watches")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q matchSlotWatchQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("entity: no matchSlotWatchQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from match_slot_watches")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for match_slot_watches")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MatchSlotWatchSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(matchSlotWatchBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), matchSlotWatchPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"match_slot_watches\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, matchSlotWatchPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from matchSlotWatch slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for match_slot_watches")
	}

	if len(matchSlotWatchAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MatchSlotWatch) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMatchSlotWatch(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MatchSlotWatchSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MatchSlotWatchSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), matchSlotWatchPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"match_slot_watches\".* FROM \"match_slot_watches\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, matchSlotWatchPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "entity: unable to reload all in MatchSlotWatchSlice")
	}

	*o = slice

	return nil
}

// MatchSlotWatchExists checks if the MatchSlotWatch row exists.
func MatchSlotWatchExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"match_slot_watches\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "entity: unable to check if match_slot_watches exists")
	}

	return exists, nil
}

// Exists checks if the MatchSlotWatch row exists.
func (o *MatchSlotWatch) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return MatchSlotWatchExists(ctx, exec, o.ID)
}
