// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// ScoringBonuse is an object representing the database table.
type ScoringBonuse struct {
	ID                 int64         `boil:"id" json:"id" toml:"id" yaml:"id"`
	FirstMessageOfDay  types.Decimal `boil:"first_message_of_day" json:"first_message_of_day" toml:"first_message_of_day" yaml:"first_message_of_day"`
	ReplyWithinSeconds int           `boil:"reply_within_seconds" json:"reply_within_seconds" toml:"reply_within_seconds" yaml:"reply_within_seconds"`
	ReplyBonus         types.Decimal `boil:"reply_bonus" json:"reply_bonus" toml:"reply_bonus" yaml:"reply_bonus"`
	CreatedAt          time.Time     `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *scoringBonuseR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L scoringBonuseL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ScoringBonuseColumns = struct {
	ID                 string
	FirstMessageOfDay  string
	ReplyWithinSeconds string
	ReplyBonus         string
	CreatedAt          string
}{
	ID:                 "id",
	FirstMessageOfDay:  "first_message_of_day",
	ReplyWithinSeconds: "reply_within_seconds",
	ReplyBonus:         "reply_bonus",
	CreatedAt:          "created_at",
}

var ScoringBonuseTableColumns = struct {
	ID                 string
	FirstMessageOfDay  string
	ReplyWithinSeconds string
	ReplyBonus         string
	CreatedAt          string
}{
	ID:                 "scoring_bonuses.id",
	FirstMessageOfDay:  "scoring_bonuses.first_message_of_day",
	ReplyWithinSeconds: "scoring_bonuses.reply_within_seconds",
	ReplyBonus:         "scoring_bonuses.reply_bonus",
	CreatedAt:          "scoring_bonuses.created_at",
}

// Generated where

type whereHelpertypes_Decimal struct{ field string }

func (w whereHelpertypes_Decimal) EQ(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertypes_Decimal) NEQ(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertypes_Decimal) LT(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertypes_Decimal) LTE(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertypes_Decimal) GT(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertypes_Decimal) GTE(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var ScoringBonuseWhere = struct {
	ID                 whereHelperint64
	FirstMessageOfDay  whereHelpertypes_Decimal
	ReplyWithinSeconds whereHelperint
	ReplyBonus         whereHelpertypes_Decimal
	CreatedAt          whereHelpertime_Time
}{
	ID:                 whereHelperint64{field: "\"scoring_bonuses\".\"id\""},
	FirstMessageOfDay:  whereHelpertypes_Decimal{field: "\"scoring_bonuses\".\"first_message_of_day\""},
	ReplyWithinSeconds: whereHelperint{field: "\"scoring_bonuses\".\"reply_within_seconds\""},
	ReplyBonus:         whereHelpertypes_Decimal{field: "\"scoring_bonuses\".\"reply_bonus\""},
	CreatedAt:          whereHelpertime_Time{field: "\"scoring_bonuses\".\"created_at\""},
}

// ScoringBonuseRels is where relationship names are stored.
var ScoringBonuseRels = struct {
}{}

// scoringBonuseR is where relationships are stored.
type scoringBonuseR struct {
}

// NewStruct creates a new relationship struct
func (*scoringBonuseR) NewStruct() *scoringBonuseR {
	return &scoringBonuseR{}
}

// scoringBonuseL is where Load methods for each relationship are stored.
type scoringBonuseL struct{}

var (
	scoringBonuseAllColumns            = []string{"id", "first_message_of_day", "reply_within_seconds", "reply_bonus", "created_at"}
	scoringBonuseColumnsWithoutDefault = []string{"first_message_of_day", "reply_within_seconds", "reply_bonus"}
	scoringBonuseColumnsWithDefault    = []string{"id", "created_at"}
	scoringBonusePrimaryKeyColumns     = []string{"id"}
	scoringBonuseGeneratedColumns      = []string{}
)

type (
	// ScoringBonuseSlice is an alias for a slice of pointers to ScoringBonuse.
	// This should almost always be used instead of []ScoringBonuse.
	ScoringBonuseSlice []*ScoringBonuse
	// ScoringBonuseHook is the signature for custom ScoringBonuse hook methods
	ScoringBonuseHook func(context.Context, boil.ContextExecutor, *ScoringBonuse) error

	scoringBonuseQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	scoringBonuseType                 = reflect.TypeOf(&ScoringBonuse{})
	scoringBonuseMapping              = queries.MakeStructMapping(scoringBonuseType)
	scoringBonusePrimaryKeyMapping, _ = queries.BindMapping(scoringBonuseType, scoringBonuseMapping, scoringBonusePrimaryKeyColumns)
	scoringBonuseInsertCacheMut       sync.RWMutex
	scoringBonuseInsertCache          = make(map[string]insertCache)
	scoringBonuseUpdateCacheMut       sync.RWMutex
	scoringBonuseUpdateCache          = make(map[string]updateCache)
	scoringBonuseUpsertCacheMut       sync.RWMutex
	scoringBonuseUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var scoringBonuseAfterSelectMu sync.Mutex
var scoringBonuseAfterSelectHooks []ScoringBonuseHook

var scoringBonuseBeforeInsertMu sync.Mutex
var scoringBonuseBeforeInsertHooks []ScoringBonuseHook
var scoringBonuseAfterInsertMu sync.Mutex
var scoringBonuseAfterInsertHooks []ScoringBonuseHook

var scoringBonuseBeforeUpdateMu sync.Mutex
var scoringBonuseBeforeUpdateHooks []ScoringBonuseHook
var scoringBonuseAfterUpdateMu sync.Mutex
var scoringBonuseAfterUpdateHooks []ScoringBonuseHook

var scoringBonuseBeforeDeleteMu sync.Mutex
var scoringBonuseBeforeDeleteHooks []ScoringBonuseHook
var scoringBonuseAfterDeleteMu sync.Mutex
var scoringBonuseAfterDeleteHooks []ScoringBonuseHook

var scoringBonuseBeforeUpsertMu sync.Mutex
var scoringBonuseBeforeUpsertHooks []ScoringBonuseHook
var scoringBonuseAfterUpsertMu sync.Mutex
var scoringBonuseAfterUpsertHooks []ScoringBonuseHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ScoringBonuse) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range scoringBonuseAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ScoringBonuse) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range scoringBonuseBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ScoringBonuse) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range scoringBonuseAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ScoringBonuse) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range scoringBonuseBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ScoringBonuse) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range scoringBonuseAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ScoringBonuse) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range scoringBonuseBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ScoringBonuse) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range scoringBonuseAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ScoringBonuse) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range scoringBonuseBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ScoringBonuse) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range scoringBonuseAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddScoringBonuseHook registers your hook function for all future operations.
func AddScoringBonuseHook(hookPoint boil.HookPoint, scoringBonuseHook ScoringBonuseHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		scoringBonuseAfterSelectMu.Lock()
		scoringBonuseAfterSelectHooks = append(scoringBonuseAfterSelectHooks, scoringBonuseHook)
		scoringBonuseAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		scoringBonuseBeforeInsertMu.Lock()
		scoringBonuseBeforeInsertHooks = append(scoringBonuseBeforeInsertHooks, scoringBonuseHook)
		scoringBonuseBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		scoringBonuseAfterInsertMu.Lock()
		scoringBonuseAfterInsertHooks = append(scoringBonuseAfterInsertHooks, scoringBonuseHook)
		scoringBonuseAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		scoringBonuseBeforeUpdateMu.Lock()
		scoringBonuseBeforeUpdateHooks = append(scoringBonuseBeforeUpdateHooks, scoringBonuseHook)
		scoringBonuseBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		scoringBonuseAfterUpdateMu.Lock()
		scoringBonuseAfterUpdateHooks = append(scoringBonuseAfterUpdateHooks, scoringBonuseHook)
		scoringBonuseAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		scoringBonuseBeforeDeleteMu.Lock()
		scoringBonuseBeforeDeleteHooks = append(scoringBonuseBeforeDeleteHooks, scoringBonuseHook)
		scoringBonuseBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		scoringBonuseAfterDeleteMu.Lock()
		scoringBonuseAfterDeleteHooks = append(scoringBonuseAfterDeleteHooks, scoringBonuseHook)
		scoringBonuseAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		scoringBonuseBeforeUpsertMu.Lock()
		scoringBonuseBeforeUpsertHooks = append(scoringBonuseBeforeUpsertHooks, scoringBonuseHook)
		scoringBonuseBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		scoringBonuseAfterUpsertMu.Lock()
		scoringBonuseAfterUpsertHooks = append(scoringBonuseAfterUpsertHooks, scoringBonuseHook)
		scoringBonuseAfterUpsertMu.Unlock()
	}
}

// One returns a single scoringBonuse record from the query.
func (q scoringBonuseQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ScoringBonuse, error) {
	o := &ScoringBonuse{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: failed to execute a one query for scoring_bonuses")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ScoringBonuse records from the query.
func (q scoringBonuseQuery) All(ctx context.Context, exec boil.ContextExecutor) (ScoringBonuseSlice, error) {
	var o []*ScoringBonuse

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "entity: failed to assign all query results to ScoringBonuse slice")
	}

	if len(scoringBonuseAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ScoringBonuse records in the query.
func (q scoringBonuseQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to count scoring_bonuses rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q scoringBonuseQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "entity: failed to check if scoring_bonuses exists")
	}

	return count > 0, nil
}

// ScoringBonuses retrieves all the records using an executor.
func ScoringBonuses(mods ...qm.QueryMod) scoringBonuseQuery {
	mods = append(mods, qm.From("\"scoring_bonuses\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"scoring_bonuses\".*"})
	}

	return scoringBonuseQuery{q}
}

// FindScoringBonuse retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindScoringBonuse(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*ScoringBonuse, error) {
	scoringBonuseObj := &ScoringBonuse{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"scoring_bonuses\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, scoringBonuseObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: unable to select from scoring_bonuses")
	}

	if err = scoringBonuseObj.doAfterSelectHooks(ctx, exec); err != nil {
		return scoringBonuseObj, err
	}

	return scoringBonuseObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ScoringBonuse) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("entity: no scoring_bonuses provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(scoringBonuseColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	scoringBonuseInsertCacheMut.RLock()
	cache, cached := scoringBonuseInsertCache[key]
	scoringBonuseInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			scoringBonuseAllColumns,
			scoringBonuseColumnsWithDefault,
			scoringBonuseColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(scoringBonuseType, scoringBonuseMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(scoringBonuseType, scoringBonuseMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"scoring_bonuses\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"scoring_bonuses\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "entity: unable to insert into scoring_bonuses")
	}

	if !cached {
		scoringBonuseInsertCacheMut.Lock()
		scoringBonuseInsertCache[key] = cache
		scoringBonuseInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ScoringBonuse.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ScoringBonuse) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	scoringBonuseUpdateCacheMut.RLock()
	cache, cached := scoringBonuseUpdateCache[key]
	scoringBonuseUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			scoringBonuseAllColumns,
			scoringBonusePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("entity: unable to update scoring_bonuses, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"scoring_bonuses\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, scoringBonusePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(scoringBonuseType, scoringBonuseMapping, append(wl, scoringBonusePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update scoring_bonuses row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by update for scoring_bonuses")
	}

	if !cached {
		scoringBonuseUpdateCacheMut.Lock()
		scoringBonuseUpdateCache[key] = cache
		scoringBonuseUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q scoringBonuseQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all for scoring_bonuses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected for scoring_bonuses")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ScoringBonuseSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("entity: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), scoringBonusePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"scoring_bonuses\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, scoringBonusePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all in scoringBonuse slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected all in update all scoringBonuse")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ScoringBonuse) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("entity: no scoring_bonuses provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(scoringBonuseColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	scoringBonuseUpsertCacheMut.RLock()
	cache, cached := scoringBonuseUpsertCache[key]
	scoringBonuseUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			scoringBonuseAllColumns,
			scoringBonuseColumnsWithDefault,
			scoringBonuseColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			scoringBonuseAllColumns,
			scoringBonusePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("entity: unable to upsert scoring_bonuses, could not build update column list")
		}

		ret := strmangle.SetComplement(scoringBonuseAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(scoringBonusePrimaryKeyColumns) == 0 {
				return errors.New("entity: unable to upsert scoring_bonuses, could not build conflict column list")
			}

			conflict = make([]string, len(scoringBonusePrimaryKeyColumns))
			copy(conflict, scoringBonusePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"scoring_bonuses\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(scoringBonuseType, scoringBonuseMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(scoringBonuseType, scoringBonuseMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "entity: unable to upsert scoring_bonuses")
	}

	if !cached {
		scoringBonuseUpsertCacheMut.Lock()
		scoringBonuseUpsertCache[key] = cache
		scoringBonuseUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ScoringBonuse record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ScoringBonuse) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("entity: no ScoringBonuse provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), scoringBonusePrimaryKeyMapping)
	sql := "DELETE FROM \"scoring_bonuses\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete from scoring_bonuses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by delete for scoring_bonuses")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q scoringBonuseQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("entity: no scoringBonuseQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from scoring_bonuses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for scoring_bonuses")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ScoringBonuseSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(scoringBonuseBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), scoringBonusePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"scoring_bonuses\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, scoringBonusePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from scoringBonuse slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for scoring_bonuses")
	}

	if len(scoringBonuseAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ScoringBonuse) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindScoringBonuse(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ScoringBonuseSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ScoringBonuseSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), scoringBonusePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"scoring_bonuses\".* FROM \"scoring_bonuses\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, scoringBonusePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "entity: unable to reload all in ScoringBonuseSlice")
	}

	*o = slice

	return nil
}

// ScoringBonuseExists checks if the ScoringBonuse row exists.
func ScoringBonuseExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"scoring_bonuses\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "entity: unable to check if scoring_bonuses exists")
	}

	return exists, nil
}

// Exists checks if the ScoringBonuse row exists.
func (o *ScoringBonuse) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ScoringBonuseExists(ctx, exec, o.ID)
}
