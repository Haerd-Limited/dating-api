// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// ImportanceWeight is an object representing the database table.
type ImportanceWeight struct {
	Key    string `boil:"key" json:"key" toml:"key" yaml:"key"`
	Weight int    `boil:"weight" json:"weight" toml:"weight" yaml:"weight"`

	R *importanceWeightR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L importanceWeightL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ImportanceWeightColumns = struct {
	Key    string
	Weight string
}{
	Key:    "key",
	Weight: "weight",
}

var ImportanceWeightTableColumns = struct {
	Key    string
	Weight string
}{
	Key:    "importance_weights.key",
	Weight: "importance_weights.weight",
}

// Generated where

var ImportanceWeightWhere = struct {
	Key    whereHelperstring
	Weight whereHelperint
}{
	Key:    whereHelperstring{field: "\"importance_weights\".\"key\""},
	Weight: whereHelperint{field: "\"importance_weights\".\"weight\""},
}

// ImportanceWeightRels is where relationship names are stored.
var ImportanceWeightRels = struct {
}{}

// importanceWeightR is where relationships are stored.
type importanceWeightR struct {
}

// NewStruct creates a new relationship struct
func (*importanceWeightR) NewStruct() *importanceWeightR {
	return &importanceWeightR{}
}

// importanceWeightL is where Load methods for each relationship are stored.
type importanceWeightL struct{}

var (
	importanceWeightAllColumns            = []string{"key", "weight"}
	importanceWeightColumnsWithoutDefault = []string{"key", "weight"}
	importanceWeightColumnsWithDefault    = []string{}
	importanceWeightPrimaryKeyColumns     = []string{"key"}
	importanceWeightGeneratedColumns      = []string{}
)

type (
	// ImportanceWeightSlice is an alias for a slice of pointers to ImportanceWeight.
	// This should almost always be used instead of []ImportanceWeight.
	ImportanceWeightSlice []*ImportanceWeight
	// ImportanceWeightHook is the signature for custom ImportanceWeight hook methods
	ImportanceWeightHook func(context.Context, boil.ContextExecutor, *ImportanceWeight) error

	importanceWeightQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	importanceWeightType                 = reflect.TypeOf(&ImportanceWeight{})
	importanceWeightMapping              = queries.MakeStructMapping(importanceWeightType)
	importanceWeightPrimaryKeyMapping, _ = queries.BindMapping(importanceWeightType, importanceWeightMapping, importanceWeightPrimaryKeyColumns)
	importanceWeightInsertCacheMut       sync.RWMutex
	importanceWeightInsertCache          = make(map[string]insertCache)
	importanceWeightUpdateCacheMut       sync.RWMutex
	importanceWeightUpdateCache          = make(map[string]updateCache)
	importanceWeightUpsertCacheMut       sync.RWMutex
	importanceWeightUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var importanceWeightAfterSelectMu sync.Mutex
var importanceWeightAfterSelectHooks []ImportanceWeightHook

var importanceWeightBeforeInsertMu sync.Mutex
var importanceWeightBeforeInsertHooks []ImportanceWeightHook
var importanceWeightAfterInsertMu sync.Mutex
var importanceWeightAfterInsertHooks []ImportanceWeightHook

var importanceWeightBeforeUpdateMu sync.Mutex
var importanceWeightBeforeUpdateHooks []ImportanceWeightHook
var importanceWeightAfterUpdateMu sync.Mutex
var importanceWeightAfterUpdateHooks []ImportanceWeightHook

var importanceWeightBeforeDeleteMu sync.Mutex
var importanceWeightBeforeDeleteHooks []ImportanceWeightHook
var importanceWeightAfterDeleteMu sync.Mutex
var importanceWeightAfterDeleteHooks []ImportanceWeightHook

var importanceWeightBeforeUpsertMu sync.Mutex
var importanceWeightBeforeUpsertHooks []ImportanceWeightHook
var importanceWeightAfterUpsertMu sync.Mutex
var importanceWeightAfterUpsertHooks []ImportanceWeightHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ImportanceWeight) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range importanceWeightAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ImportanceWeight) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range importanceWeightBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ImportanceWeight) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range importanceWeightAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ImportanceWeight) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range importanceWeightBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ImportanceWeight) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range importanceWeightAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ImportanceWeight) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range importanceWeightBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ImportanceWeight) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range importanceWeightAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ImportanceWeight) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range importanceWeightBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ImportanceWeight) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range importanceWeightAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddImportanceWeightHook registers your hook function for all future operations.
func AddImportanceWeightHook(hookPoint boil.HookPoint, importanceWeightHook ImportanceWeightHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		importanceWeightAfterSelectMu.Lock()
		importanceWeightAfterSelectHooks = append(importanceWeightAfterSelectHooks, importanceWeightHook)
		importanceWeightAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		importanceWeightBeforeInsertMu.Lock()
		importanceWeightBeforeInsertHooks = append(importanceWeightBeforeInsertHooks, importanceWeightHook)
		importanceWeightBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		importanceWeightAfterInsertMu.Lock()
		importanceWeightAfterInsertHooks = append(importanceWeightAfterInsertHooks, importanceWeightHook)
		importanceWeightAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		importanceWeightBeforeUpdateMu.Lock()
		importanceWeightBeforeUpdateHooks = append(importanceWeightBeforeUpdateHooks, importanceWeightHook)
		importanceWeightBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		importanceWeightAfterUpdateMu.Lock()
		importanceWeightAfterUpdateHooks = append(importanceWeightAfterUpdateHooks, importanceWeightHook)
		importanceWeightAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		importanceWeightBeforeDeleteMu.Lock()
		importanceWeightBeforeDeleteHooks = append(importanceWeightBeforeDeleteHooks, importanceWeightHook)
		importanceWeightBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		importanceWeightAfterDeleteMu.Lock()
		importanceWeightAfterDeleteHooks = append(importanceWeightAfterDeleteHooks, importanceWeightHook)
		importanceWeightAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		importanceWeightBeforeUpsertMu.Lock()
		importanceWeightBeforeUpsertHooks = append(importanceWeightBeforeUpsertHooks, importanceWeightHook)
		importanceWeightBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		importanceWeightAfterUpsertMu.Lock()
		importanceWeightAfterUpsertHooks = append(importanceWeightAfterUpsertHooks, importanceWeightHook)
		importanceWeightAfterUpsertMu.Unlock()
	}
}

// One returns a single importanceWeight record from the query.
func (q importanceWeightQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ImportanceWeight, error) {
	o := &ImportanceWeight{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: failed to execute a one query for importance_weights")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ImportanceWeight records from the query.
func (q importanceWeightQuery) All(ctx context.Context, exec boil.ContextExecutor) (ImportanceWeightSlice, error) {
	var o []*ImportanceWeight

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "entity: failed to assign all query results to ImportanceWeight slice")
	}

	if len(importanceWeightAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ImportanceWeight records in the query.
func (q importanceWeightQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to count importance_weights rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q importanceWeightQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "entity: failed to check if importance_weights exists")
	}

	return count > 0, nil
}

// ImportanceWeights retrieves all the records using an executor.
func ImportanceWeights(mods ...qm.QueryMod) importanceWeightQuery {
	mods = append(mods, qm.From("\"importance_weights\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"importance_weights\".*"})
	}

	return importanceWeightQuery{q}
}

// FindImportanceWeight retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindImportanceWeight(ctx context.Context, exec boil.ContextExecutor, key string, selectCols ...string) (*ImportanceWeight, error) {
	importanceWeightObj := &ImportanceWeight{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"importance_weights\" where \"key\"=$1", sel,
	)

	q := queries.Raw(query, key)

	err := q.Bind(ctx, exec, importanceWeightObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: unable to select from importance_weights")
	}

	if err = importanceWeightObj.doAfterSelectHooks(ctx, exec); err != nil {
		return importanceWeightObj, err
	}

	return importanceWeightObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ImportanceWeight) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("entity: no importance_weights provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(importanceWeightColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	importanceWeightInsertCacheMut.RLock()
	cache, cached := importanceWeightInsertCache[key]
	importanceWeightInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			importanceWeightAllColumns,
			importanceWeightColumnsWithDefault,
			importanceWeightColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(importanceWeightType, importanceWeightMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(importanceWeightType, importanceWeightMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"importance_weights\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"importance_weights\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "entity: unable to insert into importance_weights")
	}

	if !cached {
		importanceWeightInsertCacheMut.Lock()
		importanceWeightInsertCache[key] = cache
		importanceWeightInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ImportanceWeight.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ImportanceWeight) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	importanceWeightUpdateCacheMut.RLock()
	cache, cached := importanceWeightUpdateCache[key]
	importanceWeightUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			importanceWeightAllColumns,
			importanceWeightPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("entity: unable to update importance_weights, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"importance_weights\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, importanceWeightPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(importanceWeightType, importanceWeightMapping, append(wl, importanceWeightPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update importance_weights row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by update for importance_weights")
	}

	if !cached {
		importanceWeightUpdateCacheMut.Lock()
		importanceWeightUpdateCache[key] = cache
		importanceWeightUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q importanceWeightQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all for importance_weights")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected for importance_weights")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ImportanceWeightSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("entity: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), importanceWeightPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"importance_weights\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, importanceWeightPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all in importanceWeight slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected all in update all importanceWeight")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ImportanceWeight) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("entity: no importance_weights provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(importanceWeightColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	importanceWeightUpsertCacheMut.RLock()
	cache, cached := importanceWeightUpsertCache[key]
	importanceWeightUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			importanceWeightAllColumns,
			importanceWeightColumnsWithDefault,
			importanceWeightColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			importanceWeightAllColumns,
			importanceWeightPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("entity: unable to upsert importance_weights, could not build update column list")
		}

		ret := strmangle.SetComplement(importanceWeightAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(importanceWeightPrimaryKeyColumns) == 0 {
				return errors.New("entity: unable to upsert importance_weights, could not build conflict column list")
			}

			conflict = make([]string, len(importanceWeightPrimaryKeyColumns))
			copy(conflict, importanceWeightPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"importance_weights\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(importanceWeightType, importanceWeightMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(importanceWeightType, importanceWeightMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "entity: unable to upsert importance_weights")
	}

	if !cached {
		importanceWeightUpsertCacheMut.Lock()
		importanceWeightUpsertCache[key] = cache
		importanceWeightUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ImportanceWeight record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ImportanceWeight) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("entity: no ImportanceWeight provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), importanceWeightPrimaryKeyMapping)
	sql := "DELETE FROM \"importance_weights\" WHERE \"key\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete from importance_weights")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by delete for importance_weights")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q importanceWeightQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("entity: no importanceWeightQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from importance_weights")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for importance_weights")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ImportanceWeightSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(importanceWeightBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), importanceWeightPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"importance_weights\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, importanceWeightPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from importanceWeight slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for importance_weights")
	}

	if len(importanceWeightAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ImportanceWeight) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindImportanceWeight(ctx, exec, o.Key)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ImportanceWeightSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ImportanceWeightSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), importanceWeightPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"importance_weights\".* FROM \"importance_weights\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, importanceWeightPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "entity: unable to reload all in ImportanceWeightSlice")
	}

	*o = slice

	return nil
}

// ImportanceWeightExists checks if the ImportanceWeight row exists.
func ImportanceWeightExists(ctx context.Context, exec boil.ContextExecutor, key string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"importance_weights\" where \"key\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, key)
	}
	row := exec.QueryRowContext(ctx, sql, key)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "entity: unable to check if importance_weights exists")
	}

	return exists, nil
}

// Exists checks if the ImportanceWeight row exists.
func (o *ImportanceWeight) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ImportanceWeightExists(ctx, exec, o.Key)
}
