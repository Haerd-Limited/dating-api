// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// VoiceWorthHearingWeekly is an object representing the database table.
type VoiceWorthHearingWeekly struct {
	UserID       string            `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	WeekStart    time.Time         `boil:"week_start" json:"week_start" toml:"week_start" yaml:"week_start"`
	CandidateIds types.StringArray `boil:"candidate_ids" json:"candidate_ids" toml:"candidate_ids" yaml:"candidate_ids"`
	CreatedAt    time.Time         `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt    time.Time         `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *voiceWorthHearingWeeklyR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L voiceWorthHearingWeeklyL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var VoiceWorthHearingWeeklyColumns = struct {
	UserID       string
	WeekStart    string
	CandidateIds string
	CreatedAt    string
	UpdatedAt    string
}{
	UserID:       "user_id",
	WeekStart:    "week_start",
	CandidateIds: "candidate_ids",
	CreatedAt:    "created_at",
	UpdatedAt:    "updated_at",
}

var VoiceWorthHearingWeeklyTableColumns = struct {
	UserID       string
	WeekStart    string
	CandidateIds string
	CreatedAt    string
	UpdatedAt    string
}{
	UserID:       "voice_worth_hearing_weekly.user_id",
	WeekStart:    "voice_worth_hearing_weekly.week_start",
	CandidateIds: "voice_worth_hearing_weekly.candidate_ids",
	CreatedAt:    "voice_worth_hearing_weekly.created_at",
	UpdatedAt:    "voice_worth_hearing_weekly.updated_at",
}

// Generated where

var VoiceWorthHearingWeeklyWhere = struct {
	UserID       whereHelperstring
	WeekStart    whereHelpertime_Time
	CandidateIds whereHelpertypes_StringArray
	CreatedAt    whereHelpertime_Time
	UpdatedAt    whereHelpertime_Time
}{
	UserID:       whereHelperstring{field: "\"voice_worth_hearing_weekly\".\"user_id\""},
	WeekStart:    whereHelpertime_Time{field: "\"voice_worth_hearing_weekly\".\"week_start\""},
	CandidateIds: whereHelpertypes_StringArray{field: "\"voice_worth_hearing_weekly\".\"candidate_ids\""},
	CreatedAt:    whereHelpertime_Time{field: "\"voice_worth_hearing_weekly\".\"created_at\""},
	UpdatedAt:    whereHelpertime_Time{field: "\"voice_worth_hearing_weekly\".\"updated_at\""},
}

// VoiceWorthHearingWeeklyRels is where relationship names are stored.
var VoiceWorthHearingWeeklyRels = struct {
	User string
}{
	User: "User",
}

// voiceWorthHearingWeeklyR is where relationships are stored.
type voiceWorthHearingWeeklyR struct {
	User *User `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*voiceWorthHearingWeeklyR) NewStruct() *voiceWorthHearingWeeklyR {
	return &voiceWorthHearingWeeklyR{}
}

func (o *VoiceWorthHearingWeekly) GetUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetUser()
}

func (r *voiceWorthHearingWeeklyR) GetUser() *User {
	if r == nil {
		return nil
	}

	return r.User
}

// voiceWorthHearingWeeklyL is where Load methods for each relationship are stored.
type voiceWorthHearingWeeklyL struct{}

var (
	voiceWorthHearingWeeklyAllColumns            = []string{"user_id", "week_start", "candidate_ids", "created_at", "updated_at"}
	voiceWorthHearingWeeklyColumnsWithoutDefault = []string{"user_id", "week_start"}
	voiceWorthHearingWeeklyColumnsWithDefault    = []string{"candidate_ids", "created_at", "updated_at"}
	voiceWorthHearingWeeklyPrimaryKeyColumns     = []string{"user_id", "week_start"}
	voiceWorthHearingWeeklyGeneratedColumns      = []string{}
)

type (
	// VoiceWorthHearingWeeklySlice is an alias for a slice of pointers to VoiceWorthHearingWeekly.
	// This should almost always be used instead of []VoiceWorthHearingWeekly.
	VoiceWorthHearingWeeklySlice []*VoiceWorthHearingWeekly
	// VoiceWorthHearingWeeklyHook is the signature for custom VoiceWorthHearingWeekly hook methods
	VoiceWorthHearingWeeklyHook func(context.Context, boil.ContextExecutor, *VoiceWorthHearingWeekly) error

	voiceWorthHearingWeeklyQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	voiceWorthHearingWeeklyType                 = reflect.TypeOf(&VoiceWorthHearingWeekly{})
	voiceWorthHearingWeeklyMapping              = queries.MakeStructMapping(voiceWorthHearingWeeklyType)
	voiceWorthHearingWeeklyPrimaryKeyMapping, _ = queries.BindMapping(voiceWorthHearingWeeklyType, voiceWorthHearingWeeklyMapping, voiceWorthHearingWeeklyPrimaryKeyColumns)
	voiceWorthHearingWeeklyInsertCacheMut       sync.RWMutex
	voiceWorthHearingWeeklyInsertCache          = make(map[string]insertCache)
	voiceWorthHearingWeeklyUpdateCacheMut       sync.RWMutex
	voiceWorthHearingWeeklyUpdateCache          = make(map[string]updateCache)
	voiceWorthHearingWeeklyUpsertCacheMut       sync.RWMutex
	voiceWorthHearingWeeklyUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var voiceWorthHearingWeeklyAfterSelectMu sync.Mutex
var voiceWorthHearingWeeklyAfterSelectHooks []VoiceWorthHearingWeeklyHook

var voiceWorthHearingWeeklyBeforeInsertMu sync.Mutex
var voiceWorthHearingWeeklyBeforeInsertHooks []VoiceWorthHearingWeeklyHook
var voiceWorthHearingWeeklyAfterInsertMu sync.Mutex
var voiceWorthHearingWeeklyAfterInsertHooks []VoiceWorthHearingWeeklyHook

var voiceWorthHearingWeeklyBeforeUpdateMu sync.Mutex
var voiceWorthHearingWeeklyBeforeUpdateHooks []VoiceWorthHearingWeeklyHook
var voiceWorthHearingWeeklyAfterUpdateMu sync.Mutex
var voiceWorthHearingWeeklyAfterUpdateHooks []VoiceWorthHearingWeeklyHook

var voiceWorthHearingWeeklyBeforeDeleteMu sync.Mutex
var voiceWorthHearingWeeklyBeforeDeleteHooks []VoiceWorthHearingWeeklyHook
var voiceWorthHearingWeeklyAfterDeleteMu sync.Mutex
var voiceWorthHearingWeeklyAfterDeleteHooks []VoiceWorthHearingWeeklyHook

var voiceWorthHearingWeeklyBeforeUpsertMu sync.Mutex
var voiceWorthHearingWeeklyBeforeUpsertHooks []VoiceWorthHearingWeeklyHook
var voiceWorthHearingWeeklyAfterUpsertMu sync.Mutex
var voiceWorthHearingWeeklyAfterUpsertHooks []VoiceWorthHearingWeeklyHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *VoiceWorthHearingWeekly) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range voiceWorthHearingWeeklyAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *VoiceWorthHearingWeekly) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range voiceWorthHearingWeeklyBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *VoiceWorthHearingWeekly) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range voiceWorthHearingWeeklyAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *VoiceWorthHearingWeekly) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range voiceWorthHearingWeeklyBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *VoiceWorthHearingWeekly) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range voiceWorthHearingWeeklyAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *VoiceWorthHearingWeekly) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range voiceWorthHearingWeeklyBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *VoiceWorthHearingWeekly) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range voiceWorthHearingWeeklyAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *VoiceWorthHearingWeekly) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range voiceWorthHearingWeeklyBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *VoiceWorthHearingWeekly) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range voiceWorthHearingWeeklyAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddVoiceWorthHearingWeeklyHook registers your hook function for all future operations.
func AddVoiceWorthHearingWeeklyHook(hookPoint boil.HookPoint, voiceWorthHearingWeeklyHook VoiceWorthHearingWeeklyHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		voiceWorthHearingWeeklyAfterSelectMu.Lock()
		voiceWorthHearingWeeklyAfterSelectHooks = append(voiceWorthHearingWeeklyAfterSelectHooks, voiceWorthHearingWeeklyHook)
		voiceWorthHearingWeeklyAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		voiceWorthHearingWeeklyBeforeInsertMu.Lock()
		voiceWorthHearingWeeklyBeforeInsertHooks = append(voiceWorthHearingWeeklyBeforeInsertHooks, voiceWorthHearingWeeklyHook)
		voiceWorthHearingWeeklyBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		voiceWorthHearingWeeklyAfterInsertMu.Lock()
		voiceWorthHearingWeeklyAfterInsertHooks = append(voiceWorthHearingWeeklyAfterInsertHooks, voiceWorthHearingWeeklyHook)
		voiceWorthHearingWeeklyAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		voiceWorthHearingWeeklyBeforeUpdateMu.Lock()
		voiceWorthHearingWeeklyBeforeUpdateHooks = append(voiceWorthHearingWeeklyBeforeUpdateHooks, voiceWorthHearingWeeklyHook)
		voiceWorthHearingWeeklyBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		voiceWorthHearingWeeklyAfterUpdateMu.Lock()
		voiceWorthHearingWeeklyAfterUpdateHooks = append(voiceWorthHearingWeeklyAfterUpdateHooks, voiceWorthHearingWeeklyHook)
		voiceWorthHearingWeeklyAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		voiceWorthHearingWeeklyBeforeDeleteMu.Lock()
		voiceWorthHearingWeeklyBeforeDeleteHooks = append(voiceWorthHearingWeeklyBeforeDeleteHooks, voiceWorthHearingWeeklyHook)
		voiceWorthHearingWeeklyBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		voiceWorthHearingWeeklyAfterDeleteMu.Lock()
		voiceWorthHearingWeeklyAfterDeleteHooks = append(voiceWorthHearingWeeklyAfterDeleteHooks, voiceWorthHearingWeeklyHook)
		voiceWorthHearingWeeklyAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		voiceWorthHearingWeeklyBeforeUpsertMu.Lock()
		voiceWorthHearingWeeklyBeforeUpsertHooks = append(voiceWorthHearingWeeklyBeforeUpsertHooks, voiceWorthHearingWeeklyHook)
		voiceWorthHearingWeeklyBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		voiceWorthHearingWeeklyAfterUpsertMu.Lock()
		voiceWorthHearingWeeklyAfterUpsertHooks = append(voiceWorthHearingWeeklyAfterUpsertHooks, voiceWorthHearingWeeklyHook)
		voiceWorthHearingWeeklyAfterUpsertMu.Unlock()
	}
}

// One returns a single voiceWorthHearingWeekly record from the query.
func (q voiceWorthHearingWeeklyQuery) One(ctx context.Context, exec boil.ContextExecutor) (*VoiceWorthHearingWeekly, error) {
	o := &VoiceWorthHearingWeekly{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: failed to execute a one query for voice_worth_hearing_weekly")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all VoiceWorthHearingWeekly records from the query.
func (q voiceWorthHearingWeeklyQuery) All(ctx context.Context, exec boil.ContextExecutor) (VoiceWorthHearingWeeklySlice, error) {
	var o []*VoiceWorthHearingWeekly

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "entity: failed to assign all query results to VoiceWorthHearingWeekly slice")
	}

	if len(voiceWorthHearingWeeklyAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all VoiceWorthHearingWeekly records in the query.
func (q voiceWorthHearingWeeklyQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to count voice_worth_hearing_weekly rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q voiceWorthHearingWeeklyQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "entity: failed to check if voice_worth_hearing_weekly exists")
	}

	return count > 0, nil
}

// User pointed to by the foreign key.
func (o *VoiceWorthHearingWeekly) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (voiceWorthHearingWeeklyL) LoadUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVoiceWorthHearingWeekly interface{}, mods queries.Applicator) error {
	var slice []*VoiceWorthHearingWeekly
	var object *VoiceWorthHearingWeekly

	if singular {
		var ok bool
		object, ok = maybeVoiceWorthHearingWeekly.(*VoiceWorthHearingWeekly)
		if !ok {
			object = new(VoiceWorthHearingWeekly)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVoiceWorthHearingWeekly)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVoiceWorthHearingWeekly))
			}
		}
	} else {
		s, ok := maybeVoiceWorthHearingWeekly.(*[]*VoiceWorthHearingWeekly)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVoiceWorthHearingWeekly)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVoiceWorthHearingWeekly))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &voiceWorthHearingWeeklyR{}
		}
		args[object.UserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &voiceWorthHearingWeeklyR{}
			}

			args[obj.UserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.VoiceWorthHearingWeeklies = append(foreign.R.VoiceWorthHearingWeeklies, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.VoiceWorthHearingWeeklies = append(foreign.R.VoiceWorthHearingWeeklies, local)
				break
			}
		}
	}

	return nil
}

// SetUser of the voiceWorthHearingWeekly to the related item.
// Sets o.R.User to related.
// Adds o to related.R.VoiceWorthHearingWeeklies.
func (o *VoiceWorthHearingWeekly) SetUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"voice_worth_hearing_weekly\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, voiceWorthHearingWeeklyPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.UserID, o.WeekStart}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &voiceWorthHearingWeeklyR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			VoiceWorthHearingWeeklies: VoiceWorthHearingWeeklySlice{o},
		}
	} else {
		related.R.VoiceWorthHearingWeeklies = append(related.R.VoiceWorthHearingWeeklies, o)
	}

	return nil
}

// VoiceWorthHearingWeeklies retrieves all the records using an executor.
func VoiceWorthHearingWeeklies(mods ...qm.QueryMod) voiceWorthHearingWeeklyQuery {
	mods = append(mods, qm.From("\"voice_worth_hearing_weekly\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"voice_worth_hearing_weekly\".*"})
	}

	return voiceWorthHearingWeeklyQuery{q}
}

// FindVoiceWorthHearingWeekly retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindVoiceWorthHearingWeekly(ctx context.Context, exec boil.ContextExecutor, userID string, weekStart time.Time, selectCols ...string) (*VoiceWorthHearingWeekly, error) {
	voiceWorthHearingWeeklyObj := &VoiceWorthHearingWeekly{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"voice_worth_hearing_weekly\" where \"user_id\"=$1 AND \"week_start\"=$2", sel,
	)

	q := queries.Raw(query, userID, weekStart)

	err := q.Bind(ctx, exec, voiceWorthHearingWeeklyObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: unable to select from voice_worth_hearing_weekly")
	}

	if err = voiceWorthHearingWeeklyObj.doAfterSelectHooks(ctx, exec); err != nil {
		return voiceWorthHearingWeeklyObj, err
	}

	return voiceWorthHearingWeeklyObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *VoiceWorthHearingWeekly) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("entity: no voice_worth_hearing_weekly provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(voiceWorthHearingWeeklyColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	voiceWorthHearingWeeklyInsertCacheMut.RLock()
	cache, cached := voiceWorthHearingWeeklyInsertCache[key]
	voiceWorthHearingWeeklyInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			voiceWorthHearingWeeklyAllColumns,
			voiceWorthHearingWeeklyColumnsWithDefault,
			voiceWorthHearingWeeklyColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(voiceWorthHearingWeeklyType, voiceWorthHearingWeeklyMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(voiceWorthHearingWeeklyType, voiceWorthHearingWeeklyMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"voice_worth_hearing_weekly\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"voice_worth_hearing_weekly\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "entity: unable to insert into voice_worth_hearing_weekly")
	}

	if !cached {
		voiceWorthHearingWeeklyInsertCacheMut.Lock()
		voiceWorthHearingWeeklyInsertCache[key] = cache
		voiceWorthHearingWeeklyInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the VoiceWorthHearingWeekly.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *VoiceWorthHearingWeekly) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	voiceWorthHearingWeeklyUpdateCacheMut.RLock()
	cache, cached := voiceWorthHearingWeeklyUpdateCache[key]
	voiceWorthHearingWeeklyUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			voiceWorthHearingWeeklyAllColumns,
			voiceWorthHearingWeeklyPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("entity: unable to update voice_worth_hearing_weekly, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"voice_worth_hearing_weekly\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, voiceWorthHearingWeeklyPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(voiceWorthHearingWeeklyType, voiceWorthHearingWeeklyMapping, append(wl, voiceWorthHearingWeeklyPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update voice_worth_hearing_weekly row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by update for voice_worth_hearing_weekly")
	}

	if !cached {
		voiceWorthHearingWeeklyUpdateCacheMut.Lock()
		voiceWorthHearingWeeklyUpdateCache[key] = cache
		voiceWorthHearingWeeklyUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q voiceWorthHearingWeeklyQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all for voice_worth_hearing_weekly")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected for voice_worth_hearing_weekly")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o VoiceWorthHearingWeeklySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("entity: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), voiceWorthHearingWeeklyPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"voice_worth_hearing_weekly\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, voiceWorthHearingWeeklyPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all in voiceWorthHearingWeekly slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected all in update all voiceWorthHearingWeekly")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *VoiceWorthHearingWeekly) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("entity: no voice_worth_hearing_weekly provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(voiceWorthHearingWeeklyColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	voiceWorthHearingWeeklyUpsertCacheMut.RLock()
	cache, cached := voiceWorthHearingWeeklyUpsertCache[key]
	voiceWorthHearingWeeklyUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			voiceWorthHearingWeeklyAllColumns,
			voiceWorthHearingWeeklyColumnsWithDefault,
			voiceWorthHearingWeeklyColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			voiceWorthHearingWeeklyAllColumns,
			voiceWorthHearingWeeklyPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("entity: unable to upsert voice_worth_hearing_weekly, could not build update column list")
		}

		ret := strmangle.SetComplement(voiceWorthHearingWeeklyAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(voiceWorthHearingWeeklyPrimaryKeyColumns) == 0 {
				return errors.New("entity: unable to upsert voice_worth_hearing_weekly, could not build conflict column list")
			}

			conflict = make([]string, len(voiceWorthHearingWeeklyPrimaryKeyColumns))
			copy(conflict, voiceWorthHearingWeeklyPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"voice_worth_hearing_weekly\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(voiceWorthHearingWeeklyType, voiceWorthHearingWeeklyMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(voiceWorthHearingWeeklyType, voiceWorthHearingWeeklyMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "entity: unable to upsert voice_worth_hearing_weekly")
	}

	if !cached {
		voiceWorthHearingWeeklyUpsertCacheMut.Lock()
		voiceWorthHearingWeeklyUpsertCache[key] = cache
		voiceWorthHearingWeeklyUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single VoiceWorthHearingWeekly record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *VoiceWorthHearingWeekly) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("entity: no VoiceWorthHearingWeekly provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), voiceWorthHearingWeeklyPrimaryKeyMapping)
	sql := "DELETE FROM \"voice_worth_hearing_weekly\" WHERE \"user_id\"=$1 AND \"week_start\"=$2"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete from voice_worth_hearing_weekly")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by delete for voice_worth_hearing_weekly")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q voiceWorthHearingWeeklyQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("entity: no voiceWorthHearingWeeklyQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from voice_worth_hearing_weekly")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for voice_worth_hearing_weekly")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o VoiceWorthHearingWeeklySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(voiceWorthHearingWeeklyBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), voiceWorthHearingWeeklyPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"voice_worth_hearing_weekly\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, voiceWorthHearingWeeklyPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from voiceWorthHearingWeekly slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for voice_worth_hearing_weekly")
	}

	if len(voiceWorthHearingWeeklyAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *VoiceWorthHearingWeekly) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindVoiceWorthHearingWeekly(ctx, exec, o.UserID, o.WeekStart)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VoiceWorthHearingWeeklySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := VoiceWorthHearingWeeklySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), voiceWorthHearingWeeklyPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"voice_worth_hearing_weekly\".* FROM \"voice_worth_hearing_weekly\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, voiceWorthHearingWeeklyPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "entity: unable to reload all in VoiceWorthHearingWeeklySlice")
	}

	*o = slice

	return nil
}

// VoiceWorthHearingWeeklyExists checks if the VoiceWorthHearingWeekly row exists.
func VoiceWorthHearingWeeklyExists(ctx context.Context, exec boil.ContextExecutor, userID string, weekStart time.Time) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"voice_worth_hearing_weekly\" where \"user_id\"=$1 AND \"week_start\"=$2 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, userID, weekStart)
	}
	row := exec.QueryRowContext(ctx, sql, userID, weekStart)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "entity: unable to check if voice_worth_hearing_weekly exists")
	}

	return exists, nil
}

// Exists checks if the VoiceWorthHearingWeekly row exists.
func (o *VoiceWorthHearingWeekly) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return VoiceWorthHearingWeeklyExists(ctx, exec, o.UserID, o.WeekStart)
}
