// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/aarondl/randomize"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testScoringVoices(t *testing.T) {
	t.Parallel()

	query := ScoringVoices()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testScoringVoicesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringVoice{}
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ScoringVoices().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testScoringVoicesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringVoice{}
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := ScoringVoices().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ScoringVoices().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testScoringVoicesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringVoice{}
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ScoringVoiceSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ScoringVoices().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testScoringVoicesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringVoice{}
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := ScoringVoiceExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if ScoringVoice exists: %s", err)
	}
	if !e {
		t.Errorf("Expected ScoringVoiceExists to return true, but got false.")
	}
}

func testScoringVoicesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringVoice{}
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	scoringVoiceFound, err := FindScoringVoice(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if scoringVoiceFound == nil {
		t.Error("want a record, got nil")
	}
}

func testScoringVoicesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringVoice{}
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = ScoringVoices().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testScoringVoicesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringVoice{}
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := ScoringVoices().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testScoringVoicesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	scoringVoiceOne := &ScoringVoice{}
	scoringVoiceTwo := &ScoringVoice{}
	if err = randomize.Struct(seed, scoringVoiceOne, scoringVoiceDBTypes, false, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}
	if err = randomize.Struct(seed, scoringVoiceTwo, scoringVoiceDBTypes, false, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = scoringVoiceOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = scoringVoiceTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := ScoringVoices().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testScoringVoicesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	scoringVoiceOne := &ScoringVoice{}
	scoringVoiceTwo := &ScoringVoice{}
	if err = randomize.Struct(seed, scoringVoiceOne, scoringVoiceDBTypes, false, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}
	if err = randomize.Struct(seed, scoringVoiceTwo, scoringVoiceDBTypes, false, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = scoringVoiceOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = scoringVoiceTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ScoringVoices().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func scoringVoiceBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *ScoringVoice) error {
	*o = ScoringVoice{}
	return nil
}

func scoringVoiceAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *ScoringVoice) error {
	*o = ScoringVoice{}
	return nil
}

func scoringVoiceAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *ScoringVoice) error {
	*o = ScoringVoice{}
	return nil
}

func scoringVoiceBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *ScoringVoice) error {
	*o = ScoringVoice{}
	return nil
}

func scoringVoiceAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *ScoringVoice) error {
	*o = ScoringVoice{}
	return nil
}

func scoringVoiceBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *ScoringVoice) error {
	*o = ScoringVoice{}
	return nil
}

func scoringVoiceAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *ScoringVoice) error {
	*o = ScoringVoice{}
	return nil
}

func scoringVoiceBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *ScoringVoice) error {
	*o = ScoringVoice{}
	return nil
}

func scoringVoiceAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *ScoringVoice) error {
	*o = ScoringVoice{}
	return nil
}

func testScoringVoicesHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &ScoringVoice{}
	o := &ScoringVoice{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, false); err != nil {
		t.Errorf("Unable to randomize ScoringVoice object: %s", err)
	}

	AddScoringVoiceHook(boil.BeforeInsertHook, scoringVoiceBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	scoringVoiceBeforeInsertHooks = []ScoringVoiceHook{}

	AddScoringVoiceHook(boil.AfterInsertHook, scoringVoiceAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	scoringVoiceAfterInsertHooks = []ScoringVoiceHook{}

	AddScoringVoiceHook(boil.AfterSelectHook, scoringVoiceAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	scoringVoiceAfterSelectHooks = []ScoringVoiceHook{}

	AddScoringVoiceHook(boil.BeforeUpdateHook, scoringVoiceBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	scoringVoiceBeforeUpdateHooks = []ScoringVoiceHook{}

	AddScoringVoiceHook(boil.AfterUpdateHook, scoringVoiceAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	scoringVoiceAfterUpdateHooks = []ScoringVoiceHook{}

	AddScoringVoiceHook(boil.BeforeDeleteHook, scoringVoiceBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	scoringVoiceBeforeDeleteHooks = []ScoringVoiceHook{}

	AddScoringVoiceHook(boil.AfterDeleteHook, scoringVoiceAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	scoringVoiceAfterDeleteHooks = []ScoringVoiceHook{}

	AddScoringVoiceHook(boil.BeforeUpsertHook, scoringVoiceBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	scoringVoiceBeforeUpsertHooks = []ScoringVoiceHook{}

	AddScoringVoiceHook(boil.AfterUpsertHook, scoringVoiceAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	scoringVoiceAfterUpsertHooks = []ScoringVoiceHook{}
}

func testScoringVoicesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringVoice{}
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ScoringVoices().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testScoringVoicesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringVoice{}
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(strmangle.SetMerge(scoringVoicePrimaryKeyColumns, scoringVoiceColumnsWithoutDefault)...)); err != nil {
		t.Error(err)
	}

	count, err := ScoringVoices().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testScoringVoicesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringVoice{}
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testScoringVoicesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringVoice{}
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ScoringVoiceSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testScoringVoicesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ScoringVoice{}
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := ScoringVoices().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	scoringVoiceDBTypes = map[string]string{`ID`: `bigint`, `PerSecond`: `numeric`, `MaxPerNote`: `numeric`, `MinDurationSeconds`: `integer`, `CreatedAt`: `timestamp with time zone`}
	_                   = bytes.MinRead
)

func testScoringVoicesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(scoringVoicePrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(scoringVoiceAllColumns) == len(scoringVoicePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &ScoringVoice{}
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ScoringVoices().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoicePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testScoringVoicesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(scoringVoiceAllColumns) == len(scoringVoicePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &ScoringVoice{}
	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoiceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ScoringVoices().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, scoringVoiceDBTypes, true, scoringVoicePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(scoringVoiceAllColumns, scoringVoicePrimaryKeyColumns) {
		fields = scoringVoiceAllColumns
	} else {
		fields = strmangle.SetComplement(
			scoringVoiceAllColumns,
			scoringVoicePrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := ScoringVoiceSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testScoringVoicesUpsert(t *testing.T) {
	t.Parallel()

	if len(scoringVoiceAllColumns) == len(scoringVoicePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := ScoringVoice{}
	if err = randomize.Struct(seed, &o, scoringVoiceDBTypes, true); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert ScoringVoice: %s", err)
	}

	count, err := ScoringVoices().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, scoringVoiceDBTypes, false, scoringVoicePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ScoringVoice struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert ScoringVoice: %s", err)
	}

	count, err = ScoringVoices().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
