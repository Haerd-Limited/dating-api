// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// VerificationVideoSubmission is an object representing the database table.
type VerificationVideoSubmission struct {
	ID              string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID          string      `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	S3Key           string      `boil:"s3_key" json:"s3_key" toml:"s3_key" yaml:"s3_key"`
	ChallengeCode   string      `boil:"challenge_code" json:"challenge_code" toml:"challenge_code" yaml:"challenge_code"`
	Status          string      `boil:"status" json:"status" toml:"status" yaml:"status"`
	RejectionReason null.String `boil:"rejection_reason" json:"rejection_reason,omitempty" toml:"rejection_reason" yaml:"rejection_reason,omitempty"`
	CreatedAt       time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt       time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *verificationVideoSubmissionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L verificationVideoSubmissionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var VerificationVideoSubmissionColumns = struct {
	ID              string
	UserID          string
	S3Key           string
	ChallengeCode   string
	Status          string
	RejectionReason string
	CreatedAt       string
	UpdatedAt       string
}{
	ID:              "id",
	UserID:          "user_id",
	S3Key:           "s3_key",
	ChallengeCode:   "challenge_code",
	Status:          "status",
	RejectionReason: "rejection_reason",
	CreatedAt:       "created_at",
	UpdatedAt:       "updated_at",
}

var VerificationVideoSubmissionTableColumns = struct {
	ID              string
	UserID          string
	S3Key           string
	ChallengeCode   string
	Status          string
	RejectionReason string
	CreatedAt       string
	UpdatedAt       string
}{
	ID:              "verification_video_submissions.id",
	UserID:          "verification_video_submissions.user_id",
	S3Key:           "verification_video_submissions.s3_key",
	ChallengeCode:   "verification_video_submissions.challenge_code",
	Status:          "verification_video_submissions.status",
	RejectionReason: "verification_video_submissions.rejection_reason",
	CreatedAt:       "verification_video_submissions.created_at",
	UpdatedAt:       "verification_video_submissions.updated_at",
}

// Generated where

var VerificationVideoSubmissionWhere = struct {
	ID              whereHelperstring
	UserID          whereHelperstring
	S3Key           whereHelperstring
	ChallengeCode   whereHelperstring
	Status          whereHelperstring
	RejectionReason whereHelpernull_String
	CreatedAt       whereHelpertime_Time
	UpdatedAt       whereHelpertime_Time
}{
	ID:              whereHelperstring{field: "\"verification_video_submissions\".\"id\""},
	UserID:          whereHelperstring{field: "\"verification_video_submissions\".\"user_id\""},
	S3Key:           whereHelperstring{field: "\"verification_video_submissions\".\"s3_key\""},
	ChallengeCode:   whereHelperstring{field: "\"verification_video_submissions\".\"challenge_code\""},
	Status:          whereHelperstring{field: "\"verification_video_submissions\".\"status\""},
	RejectionReason: whereHelpernull_String{field: "\"verification_video_submissions\".\"rejection_reason\""},
	CreatedAt:       whereHelpertime_Time{field: "\"verification_video_submissions\".\"created_at\""},
	UpdatedAt:       whereHelpertime_Time{field: "\"verification_video_submissions\".\"updated_at\""},
}

// VerificationVideoSubmissionRels is where relationship names are stored.
var VerificationVideoSubmissionRels = struct {
	User string
}{
	User: "User",
}

// verificationVideoSubmissionR is where relationships are stored.
type verificationVideoSubmissionR struct {
	User *User `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*verificationVideoSubmissionR) NewStruct() *verificationVideoSubmissionR {
	return &verificationVideoSubmissionR{}
}

func (o *VerificationVideoSubmission) GetUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetUser()
}

func (r *verificationVideoSubmissionR) GetUser() *User {
	if r == nil {
		return nil
	}

	return r.User
}

// verificationVideoSubmissionL is where Load methods for each relationship are stored.
type verificationVideoSubmissionL struct{}

var (
	verificationVideoSubmissionAllColumns            = []string{"id", "user_id", "s3_key", "challenge_code", "status", "rejection_reason", "created_at", "updated_at"}
	verificationVideoSubmissionColumnsWithoutDefault = []string{"user_id", "s3_key", "challenge_code"}
	verificationVideoSubmissionColumnsWithDefault    = []string{"id", "status", "rejection_reason", "created_at", "updated_at"}
	verificationVideoSubmissionPrimaryKeyColumns     = []string{"id"}
	verificationVideoSubmissionGeneratedColumns      = []string{}
)

type (
	// VerificationVideoSubmissionSlice is an alias for a slice of pointers to VerificationVideoSubmission.
	// This should almost always be used instead of []VerificationVideoSubmission.
	VerificationVideoSubmissionSlice []*VerificationVideoSubmission
	// VerificationVideoSubmissionHook is the signature for custom VerificationVideoSubmission hook methods
	VerificationVideoSubmissionHook func(context.Context, boil.ContextExecutor, *VerificationVideoSubmission) error

	verificationVideoSubmissionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	verificationVideoSubmissionType                 = reflect.TypeOf(&VerificationVideoSubmission{})
	verificationVideoSubmissionMapping              = queries.MakeStructMapping(verificationVideoSubmissionType)
	verificationVideoSubmissionPrimaryKeyMapping, _ = queries.BindMapping(verificationVideoSubmissionType, verificationVideoSubmissionMapping, verificationVideoSubmissionPrimaryKeyColumns)
	verificationVideoSubmissionInsertCacheMut       sync.RWMutex
	verificationVideoSubmissionInsertCache          = make(map[string]insertCache)
	verificationVideoSubmissionUpdateCacheMut       sync.RWMutex
	verificationVideoSubmissionUpdateCache          = make(map[string]updateCache)
	verificationVideoSubmissionUpsertCacheMut       sync.RWMutex
	verificationVideoSubmissionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var verificationVideoSubmissionAfterSelectMu sync.Mutex
var verificationVideoSubmissionAfterSelectHooks []VerificationVideoSubmissionHook

var verificationVideoSubmissionBeforeInsertMu sync.Mutex
var verificationVideoSubmissionBeforeInsertHooks []VerificationVideoSubmissionHook
var verificationVideoSubmissionAfterInsertMu sync.Mutex
var verificationVideoSubmissionAfterInsertHooks []VerificationVideoSubmissionHook

var verificationVideoSubmissionBeforeUpdateMu sync.Mutex
var verificationVideoSubmissionBeforeUpdateHooks []VerificationVideoSubmissionHook
var verificationVideoSubmissionAfterUpdateMu sync.Mutex
var verificationVideoSubmissionAfterUpdateHooks []VerificationVideoSubmissionHook

var verificationVideoSubmissionBeforeDeleteMu sync.Mutex
var verificationVideoSubmissionBeforeDeleteHooks []VerificationVideoSubmissionHook
var verificationVideoSubmissionAfterDeleteMu sync.Mutex
var verificationVideoSubmissionAfterDeleteHooks []VerificationVideoSubmissionHook

var verificationVideoSubmissionBeforeUpsertMu sync.Mutex
var verificationVideoSubmissionBeforeUpsertHooks []VerificationVideoSubmissionHook
var verificationVideoSubmissionAfterUpsertMu sync.Mutex
var verificationVideoSubmissionAfterUpsertHooks []VerificationVideoSubmissionHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *VerificationVideoSubmission) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range verificationVideoSubmissionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *VerificationVideoSubmission) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range verificationVideoSubmissionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *VerificationVideoSubmission) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range verificationVideoSubmissionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *VerificationVideoSubmission) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range verificationVideoSubmissionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *VerificationVideoSubmission) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range verificationVideoSubmissionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *VerificationVideoSubmission) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range verificationVideoSubmissionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *VerificationVideoSubmission) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range verificationVideoSubmissionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *VerificationVideoSubmission) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range verificationVideoSubmissionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *VerificationVideoSubmission) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range verificationVideoSubmissionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddVerificationVideoSubmissionHook registers your hook function for all future operations.
func AddVerificationVideoSubmissionHook(hookPoint boil.HookPoint, verificationVideoSubmissionHook VerificationVideoSubmissionHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		verificationVideoSubmissionAfterSelectMu.Lock()
		verificationVideoSubmissionAfterSelectHooks = append(verificationVideoSubmissionAfterSelectHooks, verificationVideoSubmissionHook)
		verificationVideoSubmissionAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		verificationVideoSubmissionBeforeInsertMu.Lock()
		verificationVideoSubmissionBeforeInsertHooks = append(verificationVideoSubmissionBeforeInsertHooks, verificationVideoSubmissionHook)
		verificationVideoSubmissionBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		verificationVideoSubmissionAfterInsertMu.Lock()
		verificationVideoSubmissionAfterInsertHooks = append(verificationVideoSubmissionAfterInsertHooks, verificationVideoSubmissionHook)
		verificationVideoSubmissionAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		verificationVideoSubmissionBeforeUpdateMu.Lock()
		verificationVideoSubmissionBeforeUpdateHooks = append(verificationVideoSubmissionBeforeUpdateHooks, verificationVideoSubmissionHook)
		verificationVideoSubmissionBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		verificationVideoSubmissionAfterUpdateMu.Lock()
		verificationVideoSubmissionAfterUpdateHooks = append(verificationVideoSubmissionAfterUpdateHooks, verificationVideoSubmissionHook)
		verificationVideoSubmissionAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		verificationVideoSubmissionBeforeDeleteMu.Lock()
		verificationVideoSubmissionBeforeDeleteHooks = append(verificationVideoSubmissionBeforeDeleteHooks, verificationVideoSubmissionHook)
		verificationVideoSubmissionBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		verificationVideoSubmissionAfterDeleteMu.Lock()
		verificationVideoSubmissionAfterDeleteHooks = append(verificationVideoSubmissionAfterDeleteHooks, verificationVideoSubmissionHook)
		verificationVideoSubmissionAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		verificationVideoSubmissionBeforeUpsertMu.Lock()
		verificationVideoSubmissionBeforeUpsertHooks = append(verificationVideoSubmissionBeforeUpsertHooks, verificationVideoSubmissionHook)
		verificationVideoSubmissionBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		verificationVideoSubmissionAfterUpsertMu.Lock()
		verificationVideoSubmissionAfterUpsertHooks = append(verificationVideoSubmissionAfterUpsertHooks, verificationVideoSubmissionHook)
		verificationVideoSubmissionAfterUpsertMu.Unlock()
	}
}

// One returns a single verificationVideoSubmission record from the query.
func (q verificationVideoSubmissionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*VerificationVideoSubmission, error) {
	o := &VerificationVideoSubmission{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: failed to execute a one query for verification_video_submissions")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all VerificationVideoSubmission records from the query.
func (q verificationVideoSubmissionQuery) All(ctx context.Context, exec boil.ContextExecutor) (VerificationVideoSubmissionSlice, error) {
	var o []*VerificationVideoSubmission

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "entity: failed to assign all query results to VerificationVideoSubmission slice")
	}

	if len(verificationVideoSubmissionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all VerificationVideoSubmission records in the query.
func (q verificationVideoSubmissionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to count verification_video_submissions rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q verificationVideoSubmissionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "entity: failed to check if verification_video_submissions exists")
	}

	return count > 0, nil
}

// User pointed to by the foreign key.
func (o *VerificationVideoSubmission) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (verificationVideoSubmissionL) LoadUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVerificationVideoSubmission interface{}, mods queries.Applicator) error {
	var slice []*VerificationVideoSubmission
	var object *VerificationVideoSubmission

	if singular {
		var ok bool
		object, ok = maybeVerificationVideoSubmission.(*VerificationVideoSubmission)
		if !ok {
			object = new(VerificationVideoSubmission)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVerificationVideoSubmission)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVerificationVideoSubmission))
			}
		}
	} else {
		s, ok := maybeVerificationVideoSubmission.(*[]*VerificationVideoSubmission)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVerificationVideoSubmission)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVerificationVideoSubmission))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &verificationVideoSubmissionR{}
		}
		args[object.UserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &verificationVideoSubmissionR{}
			}

			args[obj.UserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.VerificationVideoSubmissions = append(foreign.R.VerificationVideoSubmissions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.VerificationVideoSubmissions = append(foreign.R.VerificationVideoSubmissions, local)
				break
			}
		}
	}

	return nil
}

// SetUser of the verificationVideoSubmission to the related item.
// Sets o.R.User to related.
// Adds o to related.R.VerificationVideoSubmissions.
func (o *VerificationVideoSubmission) SetUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"verification_video_submissions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, verificationVideoSubmissionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &verificationVideoSubmissionR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			VerificationVideoSubmissions: VerificationVideoSubmissionSlice{o},
		}
	} else {
		related.R.VerificationVideoSubmissions = append(related.R.VerificationVideoSubmissions, o)
	}

	return nil
}

// VerificationVideoSubmissions retrieves all the records using an executor.
func VerificationVideoSubmissions(mods ...qm.QueryMod) verificationVideoSubmissionQuery {
	mods = append(mods, qm.From("\"verification_video_submissions\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"verification_video_submissions\".*"})
	}

	return verificationVideoSubmissionQuery{q}
}

// FindVerificationVideoSubmission retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindVerificationVideoSubmission(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*VerificationVideoSubmission, error) {
	verificationVideoSubmissionObj := &VerificationVideoSubmission{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"verification_video_submissions\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, verificationVideoSubmissionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: unable to select from verification_video_submissions")
	}

	if err = verificationVideoSubmissionObj.doAfterSelectHooks(ctx, exec); err != nil {
		return verificationVideoSubmissionObj, err
	}

	return verificationVideoSubmissionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *VerificationVideoSubmission) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("entity: no verification_video_submissions provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(verificationVideoSubmissionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	verificationVideoSubmissionInsertCacheMut.RLock()
	cache, cached := verificationVideoSubmissionInsertCache[key]
	verificationVideoSubmissionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			verificationVideoSubmissionAllColumns,
			verificationVideoSubmissionColumnsWithDefault,
			verificationVideoSubmissionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(verificationVideoSubmissionType, verificationVideoSubmissionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(verificationVideoSubmissionType, verificationVideoSubmissionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"verification_video_submissions\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"verification_video_submissions\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "entity: unable to insert into verification_video_submissions")
	}

	if !cached {
		verificationVideoSubmissionInsertCacheMut.Lock()
		verificationVideoSubmissionInsertCache[key] = cache
		verificationVideoSubmissionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the VerificationVideoSubmission.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *VerificationVideoSubmission) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	verificationVideoSubmissionUpdateCacheMut.RLock()
	cache, cached := verificationVideoSubmissionUpdateCache[key]
	verificationVideoSubmissionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			verificationVideoSubmissionAllColumns,
			verificationVideoSubmissionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("entity: unable to update verification_video_submissions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"verification_video_submissions\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, verificationVideoSubmissionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(verificationVideoSubmissionType, verificationVideoSubmissionMapping, append(wl, verificationVideoSubmissionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update verification_video_submissions row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by update for verification_video_submissions")
	}

	if !cached {
		verificationVideoSubmissionUpdateCacheMut.Lock()
		verificationVideoSubmissionUpdateCache[key] = cache
		verificationVideoSubmissionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q verificationVideoSubmissionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all for verification_video_submissions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected for verification_video_submissions")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o VerificationVideoSubmissionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("entity: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), verificationVideoSubmissionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"verification_video_submissions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, verificationVideoSubmissionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all in verificationVideoSubmission slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected all in update all verificationVideoSubmission")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *VerificationVideoSubmission) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("entity: no verification_video_submissions provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(verificationVideoSubmissionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	verificationVideoSubmissionUpsertCacheMut.RLock()
	cache, cached := verificationVideoSubmissionUpsertCache[key]
	verificationVideoSubmissionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			verificationVideoSubmissionAllColumns,
			verificationVideoSubmissionColumnsWithDefault,
			verificationVideoSubmissionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			verificationVideoSubmissionAllColumns,
			verificationVideoSubmissionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("entity: unable to upsert verification_video_submissions, could not build update column list")
		}

		ret := strmangle.SetComplement(verificationVideoSubmissionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(verificationVideoSubmissionPrimaryKeyColumns) == 0 {
				return errors.New("entity: unable to upsert verification_video_submissions, could not build conflict column list")
			}

			conflict = make([]string, len(verificationVideoSubmissionPrimaryKeyColumns))
			copy(conflict, verificationVideoSubmissionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"verification_video_submissions\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(verificationVideoSubmissionType, verificationVideoSubmissionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(verificationVideoSubmissionType, verificationVideoSubmissionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "entity: unable to upsert verification_video_submissions")
	}

	if !cached {
		verificationVideoSubmissionUpsertCacheMut.Lock()
		verificationVideoSubmissionUpsertCache[key] = cache
		verificationVideoSubmissionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single VerificationVideoSubmission record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *VerificationVideoSubmission) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("entity: no VerificationVideoSubmission provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), verificationVideoSubmissionPrimaryKeyMapping)
	sql := "DELETE FROM \"verification_video_submissions\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete from verification_video_submissions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by delete for verification_video_submissions")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q verificationVideoSubmissionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("entity: no verificationVideoSubmissionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from verification_video_submissions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for verification_video_submissions")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o VerificationVideoSubmissionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(verificationVideoSubmissionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), verificationVideoSubmissionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"verification_video_submissions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, verificationVideoSubmissionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from verificationVideoSubmission slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for verification_video_submissions")
	}

	if len(verificationVideoSubmissionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *VerificationVideoSubmission) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindVerificationVideoSubmission(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VerificationVideoSubmissionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := VerificationVideoSubmissionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), verificationVideoSubmissionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"verification_video_submissions\".* FROM \"verification_video_submissions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, verificationVideoSubmissionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "entity: unable to reload all in VerificationVideoSubmissionSlice")
	}

	*o = slice

	return nil
}

// VerificationVideoSubmissionExists checks if the VerificationVideoSubmission row exists.
func VerificationVideoSubmissionExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"verification_video_submissions\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "entity: unable to check if verification_video_submissions exists")
	}

	return exists, nil
}

// Exists checks if the VerificationVideoSubmission row exists.
func (o *VerificationVideoSubmission) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return VerificationVideoSubmissionExists(ctx, exec, o.ID)
}
