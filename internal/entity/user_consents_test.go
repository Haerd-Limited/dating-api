// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/aarondl/randomize"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testUserConsents(t *testing.T) {
	t.Parallel()

	query := UserConsents()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testUserConsentsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserConsent{}
	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserConsents().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserConsentsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserConsent{}
	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := UserConsents().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserConsents().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserConsentsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserConsent{}
	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserConsentSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserConsents().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserConsentsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserConsent{}
	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := UserConsentExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if UserConsent exists: %s", err)
	}
	if !e {
		t.Errorf("Expected UserConsentExists to return true, but got false.")
	}
}

func testUserConsentsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserConsent{}
	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	userConsentFound, err := FindUserConsent(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if userConsentFound == nil {
		t.Error("want a record, got nil")
	}
}

func testUserConsentsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserConsent{}
	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = UserConsents().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testUserConsentsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserConsent{}
	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := UserConsents().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testUserConsentsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	userConsentOne := &UserConsent{}
	userConsentTwo := &UserConsent{}
	if err = randomize.Struct(seed, userConsentOne, userConsentDBTypes, false, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}
	if err = randomize.Struct(seed, userConsentTwo, userConsentDBTypes, false, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = userConsentOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userConsentTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserConsents().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testUserConsentsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	userConsentOne := &UserConsent{}
	userConsentTwo := &UserConsent{}
	if err = randomize.Struct(seed, userConsentOne, userConsentDBTypes, false, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}
	if err = randomize.Struct(seed, userConsentTwo, userConsentDBTypes, false, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = userConsentOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userConsentTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserConsents().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func userConsentBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *UserConsent) error {
	*o = UserConsent{}
	return nil
}

func userConsentAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *UserConsent) error {
	*o = UserConsent{}
	return nil
}

func userConsentAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *UserConsent) error {
	*o = UserConsent{}
	return nil
}

func userConsentBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *UserConsent) error {
	*o = UserConsent{}
	return nil
}

func userConsentAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *UserConsent) error {
	*o = UserConsent{}
	return nil
}

func userConsentBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *UserConsent) error {
	*o = UserConsent{}
	return nil
}

func userConsentAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *UserConsent) error {
	*o = UserConsent{}
	return nil
}

func userConsentBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *UserConsent) error {
	*o = UserConsent{}
	return nil
}

func userConsentAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *UserConsent) error {
	*o = UserConsent{}
	return nil
}

func testUserConsentsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &UserConsent{}
	o := &UserConsent{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, userConsentDBTypes, false); err != nil {
		t.Errorf("Unable to randomize UserConsent object: %s", err)
	}

	AddUserConsentHook(boil.BeforeInsertHook, userConsentBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	userConsentBeforeInsertHooks = []UserConsentHook{}

	AddUserConsentHook(boil.AfterInsertHook, userConsentAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	userConsentAfterInsertHooks = []UserConsentHook{}

	AddUserConsentHook(boil.AfterSelectHook, userConsentAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	userConsentAfterSelectHooks = []UserConsentHook{}

	AddUserConsentHook(boil.BeforeUpdateHook, userConsentBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	userConsentBeforeUpdateHooks = []UserConsentHook{}

	AddUserConsentHook(boil.AfterUpdateHook, userConsentAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	userConsentAfterUpdateHooks = []UserConsentHook{}

	AddUserConsentHook(boil.BeforeDeleteHook, userConsentBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	userConsentBeforeDeleteHooks = []UserConsentHook{}

	AddUserConsentHook(boil.AfterDeleteHook, userConsentAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	userConsentAfterDeleteHooks = []UserConsentHook{}

	AddUserConsentHook(boil.BeforeUpsertHook, userConsentBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	userConsentBeforeUpsertHooks = []UserConsentHook{}

	AddUserConsentHook(boil.AfterUpsertHook, userConsentAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	userConsentAfterUpsertHooks = []UserConsentHook{}
}

func testUserConsentsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserConsent{}
	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserConsents().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserConsentsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserConsent{}
	if err = randomize.Struct(seed, o, userConsentDBTypes, true); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(strmangle.SetMerge(userConsentPrimaryKeyColumns, userConsentColumnsWithoutDefault)...)); err != nil {
		t.Error(err)
	}

	count, err := UserConsents().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserConsentToOneUserUsingUser(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local UserConsent
	var foreign User

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, userConsentDBTypes, false, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, userDBTypes, false, userColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize User struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.UserID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.User().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	ranAfterSelectHook := false
	AddUserHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *User) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := UserConsentSlice{&local}
	if err = local.L.LoadUser(ctx, tx, false, (*[]*UserConsent)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.User = nil
	if err = local.L.LoadUser(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testUserConsentToOneSetOpUserUsingUser(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserConsent
	var b, c User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userConsentDBTypes, false, strmangle.SetComplement(userConsentPrimaryKeyColumns, userConsentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*User{&b, &c} {
		err = a.SetUser(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.User != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.UserConsents[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.UserID != x.ID {
			t.Error("foreign key was wrong value", a.UserID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.UserID))
		reflect.Indirect(reflect.ValueOf(&a.UserID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.UserID != x.ID {
			t.Error("foreign key was wrong value", a.UserID, x.ID)
		}
	}
}

func testUserConsentsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserConsent{}
	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testUserConsentsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserConsent{}
	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserConsentSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testUserConsentsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserConsent{}
	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserConsents().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	userConsentDBTypes = map[string]string{`ID`: `uuid`, `UserID`: `uuid`, `ConsentType`: `text`, `Version`: `text`, `Accepted`: `boolean`, `AcceptedAt`: `timestamp with time zone`, `RevokedAt`: `timestamp with time zone`, `IP`: `inet`, `UserAgent`: `text`}
	_                  = bytes.MinRead
)

func testUserConsentsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(userConsentPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(userConsentAllColumns) == len(userConsentPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserConsent{}
	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserConsents().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testUserConsentsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(userConsentAllColumns) == len(userConsentPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserConsent{}
	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserConsents().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userConsentDBTypes, true, userConsentPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(userConsentAllColumns, userConsentPrimaryKeyColumns) {
		fields = userConsentAllColumns
	} else {
		fields = strmangle.SetComplement(
			userConsentAllColumns,
			userConsentPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := UserConsentSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testUserConsentsUpsert(t *testing.T) {
	t.Parallel()

	if len(userConsentAllColumns) == len(userConsentPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := UserConsent{}
	if err = randomize.Struct(seed, &o, userConsentDBTypes, true); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserConsent: %s", err)
	}

	count, err := UserConsents().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, userConsentDBTypes, false, userConsentPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserConsent struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserConsent: %s", err)
	}

	count, err = UserConsents().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
