// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/aarondl/randomize"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testUserThemes(t *testing.T) {
	t.Parallel()

	query := UserThemes()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testUserThemesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTheme{}
	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserThemes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserThemesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTheme{}
	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := UserThemes().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserThemes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserThemesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTheme{}
	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserThemeSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserThemes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserThemesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTheme{}
	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := UserThemeExists(ctx, tx, o.UserID)
	if err != nil {
		t.Errorf("Unable to check if UserTheme exists: %s", err)
	}
	if !e {
		t.Errorf("Expected UserThemeExists to return true, but got false.")
	}
}

func testUserThemesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTheme{}
	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	userThemeFound, err := FindUserTheme(ctx, tx, o.UserID)
	if err != nil {
		t.Error(err)
	}

	if userThemeFound == nil {
		t.Error("want a record, got nil")
	}
}

func testUserThemesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTheme{}
	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = UserThemes().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testUserThemesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTheme{}
	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := UserThemes().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testUserThemesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	userThemeOne := &UserTheme{}
	userThemeTwo := &UserTheme{}
	if err = randomize.Struct(seed, userThemeOne, userThemeDBTypes, false, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}
	if err = randomize.Struct(seed, userThemeTwo, userThemeDBTypes, false, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = userThemeOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userThemeTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserThemes().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testUserThemesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	userThemeOne := &UserTheme{}
	userThemeTwo := &UserTheme{}
	if err = randomize.Struct(seed, userThemeOne, userThemeDBTypes, false, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}
	if err = randomize.Struct(seed, userThemeTwo, userThemeDBTypes, false, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = userThemeOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userThemeTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserThemes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func userThemeBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *UserTheme) error {
	*o = UserTheme{}
	return nil
}

func userThemeAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *UserTheme) error {
	*o = UserTheme{}
	return nil
}

func userThemeAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *UserTheme) error {
	*o = UserTheme{}
	return nil
}

func userThemeBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *UserTheme) error {
	*o = UserTheme{}
	return nil
}

func userThemeAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *UserTheme) error {
	*o = UserTheme{}
	return nil
}

func userThemeBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *UserTheme) error {
	*o = UserTheme{}
	return nil
}

func userThemeAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *UserTheme) error {
	*o = UserTheme{}
	return nil
}

func userThemeBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *UserTheme) error {
	*o = UserTheme{}
	return nil
}

func userThemeAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *UserTheme) error {
	*o = UserTheme{}
	return nil
}

func testUserThemesHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &UserTheme{}
	o := &UserTheme{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, userThemeDBTypes, false); err != nil {
		t.Errorf("Unable to randomize UserTheme object: %s", err)
	}

	AddUserThemeHook(boil.BeforeInsertHook, userThemeBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	userThemeBeforeInsertHooks = []UserThemeHook{}

	AddUserThemeHook(boil.AfterInsertHook, userThemeAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	userThemeAfterInsertHooks = []UserThemeHook{}

	AddUserThemeHook(boil.AfterSelectHook, userThemeAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	userThemeAfterSelectHooks = []UserThemeHook{}

	AddUserThemeHook(boil.BeforeUpdateHook, userThemeBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	userThemeBeforeUpdateHooks = []UserThemeHook{}

	AddUserThemeHook(boil.AfterUpdateHook, userThemeAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	userThemeAfterUpdateHooks = []UserThemeHook{}

	AddUserThemeHook(boil.BeforeDeleteHook, userThemeBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	userThemeBeforeDeleteHooks = []UserThemeHook{}

	AddUserThemeHook(boil.AfterDeleteHook, userThemeAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	userThemeAfterDeleteHooks = []UserThemeHook{}

	AddUserThemeHook(boil.BeforeUpsertHook, userThemeBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	userThemeBeforeUpsertHooks = []UserThemeHook{}

	AddUserThemeHook(boil.AfterUpsertHook, userThemeAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	userThemeAfterUpsertHooks = []UserThemeHook{}
}

func testUserThemesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTheme{}
	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserThemes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserThemesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTheme{}
	if err = randomize.Struct(seed, o, userThemeDBTypes, true); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(strmangle.SetMerge(userThemePrimaryKeyColumns, userThemeColumnsWithoutDefault)...)); err != nil {
		t.Error(err)
	}

	count, err := UserThemes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserThemeToOneUserUsingUser(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local UserTheme
	var foreign User

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, userThemeDBTypes, false, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, userDBTypes, false, userColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize User struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.UserID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.User().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	ranAfterSelectHook := false
	AddUserHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *User) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := UserThemeSlice{&local}
	if err = local.L.LoadUser(ctx, tx, false, (*[]*UserTheme)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.User = nil
	if err = local.L.LoadUser(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testUserThemeToOneSetOpUserUsingUser(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserTheme
	var b, c User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userThemeDBTypes, false, strmangle.SetComplement(userThemePrimaryKeyColumns, userThemeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*User{&b, &c} {
		err = a.SetUser(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.User != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.UserTheme != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.UserID != x.ID {
			t.Error("foreign key was wrong value", a.UserID)
		}

		if exists, err := UserThemeExists(ctx, tx, a.UserID); err != nil {
			t.Fatal(err)
		} else if !exists {
			t.Error("want 'a' to exist")
		}

	}
}

func testUserThemesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTheme{}
	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testUserThemesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTheme{}
	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserThemeSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testUserThemesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTheme{}
	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserThemes().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	userThemeDBTypes = map[string]string{`UserID`: `uuid`, `BaseHex`: `text`, `Palette`: `jsonb`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`}
	_                = bytes.MinRead
)

func testUserThemesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(userThemePrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(userThemeAllColumns) == len(userThemePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserTheme{}
	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserThemes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testUserThemesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(userThemeAllColumns) == len(userThemePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserTheme{}
	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserThemes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userThemeDBTypes, true, userThemePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(userThemeAllColumns, userThemePrimaryKeyColumns) {
		fields = userThemeAllColumns
	} else {
		fields = strmangle.SetComplement(
			userThemeAllColumns,
			userThemePrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := UserThemeSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testUserThemesUpsert(t *testing.T) {
	t.Parallel()

	if len(userThemeAllColumns) == len(userThemePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := UserTheme{}
	if err = randomize.Struct(seed, &o, userThemeDBTypes, true); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserTheme: %s", err)
	}

	count, err := UserThemes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, userThemeDBTypes, false, userThemePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserTheme struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserTheme: %s", err)
	}

	count, err = UserThemes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
