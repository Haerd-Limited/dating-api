// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// VSwipeEvent is an object representing the database table.
type VSwipeEvent struct {
	ID         null.String `boil:"id" json:"id,omitempty" toml:"id" yaml:"id,omitempty"`
	OccurredAt null.Time   `boil:"occurred_at" json:"occurred_at,omitempty" toml:"occurred_at" yaml:"occurred_at,omitempty"`
	ActorID    null.String `boil:"actor_id" json:"actor_id,omitempty" toml:"actor_id" yaml:"actor_id,omitempty"`
	Action     null.String `boil:"action" json:"action,omitempty" toml:"action" yaml:"action,omitempty"`
	PromptID   null.Int64  `boil:"prompt_id" json:"prompt_id,omitempty" toml:"prompt_id" yaml:"prompt_id,omitempty"`
}

var VSwipeEventColumns = struct {
	ID         string
	OccurredAt string
	ActorID    string
	Action     string
	PromptID   string
}{
	ID:         "id",
	OccurredAt: "occurred_at",
	ActorID:    "actor_id",
	Action:     "action",
	PromptID:   "prompt_id",
}

var VSwipeEventTableColumns = struct {
	ID         string
	OccurredAt string
	ActorID    string
	Action     string
	PromptID   string
}{
	ID:         "v_swipe_events.id",
	OccurredAt: "v_swipe_events.occurred_at",
	ActorID:    "v_swipe_events.actor_id",
	Action:     "v_swipe_events.action",
	PromptID:   "v_swipe_events.prompt_id",
}

// Generated where

var VSwipeEventWhere = struct {
	ID         whereHelpernull_String
	OccurredAt whereHelpernull_Time
	ActorID    whereHelpernull_String
	Action     whereHelpernull_String
	PromptID   whereHelpernull_Int64
}{
	ID:         whereHelpernull_String{field: "\"v_swipe_events\".\"id\""},
	OccurredAt: whereHelpernull_Time{field: "\"v_swipe_events\".\"occurred_at\""},
	ActorID:    whereHelpernull_String{field: "\"v_swipe_events\".\"actor_id\""},
	Action:     whereHelpernull_String{field: "\"v_swipe_events\".\"action\""},
	PromptID:   whereHelpernull_Int64{field: "\"v_swipe_events\".\"prompt_id\""},
}

var (
	vSwipeEventAllColumns            = []string{"id", "occurred_at", "actor_id", "action", "prompt_id"}
	vSwipeEventColumnsWithoutDefault = []string{}
	vSwipeEventColumnsWithDefault    = []string{"id", "occurred_at", "actor_id", "action", "prompt_id"}
	vSwipeEventPrimaryKeyColumns     = []string{}
	vSwipeEventGeneratedColumns      = []string{}
)

type (
	// VSwipeEventSlice is an alias for a slice of pointers to VSwipeEvent.
	// This should almost always be used instead of []VSwipeEvent.
	VSwipeEventSlice []*VSwipeEvent
	// VSwipeEventHook is the signature for custom VSwipeEvent hook methods
	VSwipeEventHook func(context.Context, boil.ContextExecutor, *VSwipeEvent) error

	vSwipeEventQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	vSwipeEventType           = reflect.TypeOf(&VSwipeEvent{})
	vSwipeEventMapping        = queries.MakeStructMapping(vSwipeEventType)
	vSwipeEventInsertCacheMut sync.RWMutex
	vSwipeEventInsertCache    = make(map[string]insertCache)
	vSwipeEventUpdateCacheMut sync.RWMutex
	vSwipeEventUpdateCache    = make(map[string]updateCache)
	vSwipeEventUpsertCacheMut sync.RWMutex
	vSwipeEventUpsertCache    = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
	// These are used in some views
	_ = fmt.Sprintln("")
	_ = reflect.Int
	_ = strings.Builder{}
	_ = sync.Mutex{}
	_ = strmangle.Plural("")
	_ = strconv.IntSize
)

var vSwipeEventAfterSelectMu sync.Mutex
var vSwipeEventAfterSelectHooks []VSwipeEventHook

var vSwipeEventBeforeInsertMu sync.Mutex
var vSwipeEventBeforeInsertHooks []VSwipeEventHook
var vSwipeEventAfterInsertMu sync.Mutex
var vSwipeEventAfterInsertHooks []VSwipeEventHook

var vSwipeEventBeforeUpsertMu sync.Mutex
var vSwipeEventBeforeUpsertHooks []VSwipeEventHook
var vSwipeEventAfterUpsertMu sync.Mutex
var vSwipeEventAfterUpsertHooks []VSwipeEventHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *VSwipeEvent) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range vSwipeEventAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *VSwipeEvent) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range vSwipeEventBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *VSwipeEvent) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range vSwipeEventAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *VSwipeEvent) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range vSwipeEventBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *VSwipeEvent) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range vSwipeEventAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddVSwipeEventHook registers your hook function for all future operations.
func AddVSwipeEventHook(hookPoint boil.HookPoint, vSwipeEventHook VSwipeEventHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		vSwipeEventAfterSelectMu.Lock()
		vSwipeEventAfterSelectHooks = append(vSwipeEventAfterSelectHooks, vSwipeEventHook)
		vSwipeEventAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		vSwipeEventBeforeInsertMu.Lock()
		vSwipeEventBeforeInsertHooks = append(vSwipeEventBeforeInsertHooks, vSwipeEventHook)
		vSwipeEventBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		vSwipeEventAfterInsertMu.Lock()
		vSwipeEventAfterInsertHooks = append(vSwipeEventAfterInsertHooks, vSwipeEventHook)
		vSwipeEventAfterInsertMu.Unlock()
	case boil.BeforeUpsertHook:
		vSwipeEventBeforeUpsertMu.Lock()
		vSwipeEventBeforeUpsertHooks = append(vSwipeEventBeforeUpsertHooks, vSwipeEventHook)
		vSwipeEventBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		vSwipeEventAfterUpsertMu.Lock()
		vSwipeEventAfterUpsertHooks = append(vSwipeEventAfterUpsertHooks, vSwipeEventHook)
		vSwipeEventAfterUpsertMu.Unlock()
	}
}

// One returns a single vSwipeEvent record from the query.
func (q vSwipeEventQuery) One(ctx context.Context, exec boil.ContextExecutor) (*VSwipeEvent, error) {
	o := &VSwipeEvent{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: failed to execute a one query for v_swipe_events")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all VSwipeEvent records from the query.
func (q vSwipeEventQuery) All(ctx context.Context, exec boil.ContextExecutor) (VSwipeEventSlice, error) {
	var o []*VSwipeEvent

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "entity: failed to assign all query results to VSwipeEvent slice")
	}

	if len(vSwipeEventAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all VSwipeEvent records in the query.
func (q vSwipeEventQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to count v_swipe_events rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q vSwipeEventQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "entity: failed to check if v_swipe_events exists")
	}

	return count > 0, nil
}

// VSwipeEvents retrieves all the records using an executor.
func VSwipeEvents(mods ...qm.QueryMod) vSwipeEventQuery {
	mods = append(mods, qm.From("\"v_swipe_events\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"v_swipe_events\".*"})
	}

	return vSwipeEventQuery{q}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *VSwipeEvent) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("entity: no v_swipe_events provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(vSwipeEventColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	vSwipeEventInsertCacheMut.RLock()
	cache, cached := vSwipeEventInsertCache[key]
	vSwipeEventInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			vSwipeEventAllColumns,
			vSwipeEventColumnsWithDefault,
			vSwipeEventColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(vSwipeEventType, vSwipeEventMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(vSwipeEventType, vSwipeEventMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"v_swipe_events\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"v_swipe_events\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "entity: unable to insert into v_swipe_events")
	}

	if !cached {
		vSwipeEventInsertCacheMut.Lock()
		vSwipeEventInsertCache[key] = cache
		vSwipeEventInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *VSwipeEvent) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("entity: no v_swipe_events provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(vSwipeEventColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	vSwipeEventUpsertCacheMut.RLock()
	cache, cached := vSwipeEventUpsertCache[key]
	vSwipeEventUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			vSwipeEventAllColumns,
			vSwipeEventColumnsWithDefault,
			vSwipeEventColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			vSwipeEventAllColumns,
			vSwipeEventPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("entity: unable to upsert v_swipe_events, could not build update column list")
		}

		ret := strmangle.SetComplement(vSwipeEventAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(vSwipeEventPrimaryKeyColumns) == 0 {
				return errors.New("entity: unable to upsert v_swipe_events, could not build conflict column list")
			}

			conflict = make([]string, len(vSwipeEventPrimaryKeyColumns))
			copy(conflict, vSwipeEventPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"v_swipe_events\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(vSwipeEventType, vSwipeEventMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(vSwipeEventType, vSwipeEventMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "entity: unable to upsert v_swipe_events")
	}

	if !cached {
		vSwipeEventUpsertCacheMut.Lock()
		vSwipeEventUpsertCache[key] = cache
		vSwipeEventUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}
